{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-capability",
  "description": "Huntronomer desktop capabilities. Grants the default core IPC surface so the frontend can call registered #[tauri::command] handlers and listen for events emitted by Rust.",
  "windows": ["main"],
  "platforms": ["linux", "macOS", "windows"],
  "permissions": [
    "core:default"
  ]
}
