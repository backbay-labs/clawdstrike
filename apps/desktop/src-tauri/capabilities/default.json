{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-capability",
  "description": "Huntronomer desktop capabilities. Deny-everything baseline: this app is mostly read-only and exposes only a minimal set of Tauri commands; permissions should be added explicitly as new capabilities are exercised.",
  "windows": ["main"],
  "platforms": ["linux", "macOS", "windows"],
  "permissions": []
}
