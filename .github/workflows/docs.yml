# Clawdstrike Documentation Deployment
#
# Builds and deploys the mdBook documentation site to GitHub Pages.
#
# Triggers:
# - Push to main branch (docs/** changes)
# - Manual workflow dispatch

name: Deploy Documentation

on:
  push:
    branches:
      - main
    paths:
      - "docs/**"
      - ".github/workflows/docs.yml"
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN for Pages deployment
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  # Build job
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup mdBook
        uses: peaceiris/actions-mdbook@v2
        with:
          mdbook-version: "0.5.2"

      - name: Build documentation
        run: |
          cd docs
          mdbook build

      - name: Setup Pages
        uses: actions/configure-pages@v6

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: docs/book

  # Deployment job
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

  # Link checker (runs in parallel)
  linkcheck:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Setup mdBook
        uses: peaceiris/actions-mdbook@v2
        with:
          mdbook-version: "0.5.2"

      - name: Install link checker
        run: |
          cargo install mdbook-linkcheck2 --version 0.12.0 --locked
          cat > "${HOME}/.cargo/bin/mdbook-linkcheck" <<'SH'
          #!/usr/bin/env bash
          exec mdbook-linkcheck2 "$@"
          SH
          chmod +x "${HOME}/.cargo/bin/mdbook-linkcheck"

      - name: Build with link check
        run: |
          cd docs
          mdbook build
        env:
          MDBOOK_OUTPUT__LINKCHECK: "{}"

      - name: Report broken links
        if: failure()
        run: |
          echo "::warning::Broken links detected in documentation"
