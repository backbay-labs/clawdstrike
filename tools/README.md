# Tools

Repository tooling and validators used by contributors and CI.

Current:

1. `tools/scripts/` - validation and utility scripts (for example docs validation and policy parity tooling).

Operational scripts intended for end-user workflows should live in `scripts/`.
