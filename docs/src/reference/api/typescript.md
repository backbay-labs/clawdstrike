# TypeScript API (experimental)

This repository includes a TypeScript SDK under `packages/hush-ts`.

This mdBook focuses on the Rust policy and receipt contracts. The TypeScript SDK is still evolving and may not match the Rust schema described here.

## Where to look

- `packages/hush-ts/README.md`
- `packages/hush-ts/src/`
