# Rulesets

Preconfigured policy rulesets in YAML format.

Use these as:

1. baseline policy packs for runtime enforcement.
2. references when authoring organization-specific policies.
3. fixtures for policy compatibility and regression tests.

Schema and policy behavior are documented in `docs/src/reference/policy-schema.md`.
