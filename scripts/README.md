# Scripts

Operator-facing repository scripts.

Conventions:

1. Prefer stable entrypoints here for CI/release/local orchestration.
2. Put reusable helper logic under `tools/scripts/` when it is developer-tooling specific.
3. Keep scripts idempotent and explicit about required environment variables.

Current policy/structure guardrails:

1. `scripts/path-lint.sh` - stale legacy path reference detection.
2. `scripts/move-validation.sh` - verifies moved legacy paths are no longer tracked.
3. `scripts/architecture-guardrails.sh` - verifies domain README/ownership/repo-map consistency.
4. `scripts/cleanup-legacy-paths.sh` - removes stale legacy directories from local checkouts.
5. `scripts/openclaw-agent-smoke.sh` - validates agent-owned OpenClaw flows (`health -> policy bypass toggle -> gateway connect -> request relay -> optional reconnect`).
6. `scripts/release-helm-chart.sh` - lints/templates/packages the ClawdStrike Helm chart and optionally pushes it to an OCI registry.
7. `scripts/helm-e2e-smoke.sh` - OCI install smoke check (`helm upgrade --install`, `helm test`, service health probes, diagnostics bundle).
8. `scripts/helm-resilience-security.sh` - nightly-style restart/recovery + upgrade/rollback + security checks with machine-readable summary.
9. `scripts/helm-all-on-preflight.sh` - strict preflight gate for all-on profile rollout (bridge image availability, cluster prerequisites, and render checks).
10. `scripts/openclaw-plugin-install-link-smoke.sh` - validates clean-room `plugins install --link` + `plugins enable` flow against the package entry path and emits `summary.json`.
11. `scripts/openclaw-plugin-runtime-smoke.sh` - validates OpenClaw plugin runtime loading and expected hook registrations from `plugins info ... --json`, emitting `summary.json`.
12. `scripts/openclaw-plugin-blocked-call-e2e.sh` - validates blocked runtime command execution with a side-effecting chat probe plus a direct `policy_check` denial on a canonical dangerous command, emitting `summary.json`.
13. `scripts/adaptive-openclaw-spine-e2e.sh` - combined smoke that validates OpenClaw blocked-call interception plus control-api signed Spine approval envelope emission, emitting `summary.json`.
14. `scripts/huntronomer-playwright-smoke.sh` - Huntronomer desktop shell smoke via Playwright CLI (`launch overlay -> dismiss -> Hunt Deck artifacts`), emitting `summary.json`.
15. `scripts/workbench-live-dogfood.sh` - live operator smoke for the mounted workbench shell (`settings -> local stack connect -> intel/findings/sentinel flows`), emitting `summary.json`.
16. `scripts/workbench-mission-control-dogfood.sh` - live Mission Control dogfood for the mounted workbench shell (`settings -> sentinel creation -> blocked Claude launch -> ready OpenClaw launch`), emitting `summary.json`.
17. `scripts/network-extension-live-dogfood.sh` - live macOS NetworkExtension containment dogfood (`seed network-flow graph -> execute restrict_egress -> trigger real flow -> require strict liveEnforcementProven proof -> rollback -> verify the summary artifact`), emitting `summary.json`.
18. `scripts/endpoint-security-live-dogfood.sh` - live macOS EndpointSecurity delivery dogfood (`optionally start a managed AUTH_OPEN observer/status tool -> check provider health, allowing only the first-signal bootstrap state -> trigger benign file/process probe -> require provider-delivered flight-recorder/causal-graph observation plus matching EndpointSecurity observation receipt -> verify the summary artifact`), emitting `summary.json`.
19. `scripts/macos-provider-live-dogfood.sh` - one-command deployed macOS provider gate (`signed/notarized deployment evidence -> EndpointSecurity dogfood -> NetworkExtension dogfood -> combined gate -> recursive hash manifest -> saved-gate and fresh-gate manifest verification`), passing one run ID through all child artifacts, writing `manifest.json` with explicit `evidenceMode=live`, and emitting `gate-result.json`; `--preflight` validates required commands, child tools, target syntax, run ID shape, numeric inputs, output-root safety, empty-or-explicit-replace output semantics, and the signed `.app` bundle path without collecting evidence or running live ES/NE dogfood, and `--self-test` regression-checks the preflight path without live provider side effects.
20. `scripts/endpoint-decision-engine-live-qualification.sh` - QA-host live qualification driver that fixes the final evidence bundle layout, preflights the signed app plus external target, enforces that provider/proof/output paths stay inside the bundle without overlapping, creates runtime output parent directories only after preflight, optionally builds the supplemental proof root from explicit `CLAWDSTRIKE_EDE_*` source artifact variables with explicit `evidenceMode=live`, runs the macOS provider dogfood into `macos-provider/`, then runs `scripts/endpoint-decision-engine-qualification-bundle.py`; it records bounded driver metadata and a self-hashed top-level `qualification-summary.json`, preserves the qualification verifier exit code while logging summary/audit paths on failure, does not fabricate supplemental proofs, so missing proof inputs keep the final audit red, `CLAWDSTRIKE_EDE_MACOS_PROVIDER_REPLACE_OUTPUT=1` is required to overwrite stale macOS provider evidence, and `CLAWDSTRIKE_EDE_SUPPLEMENTAL_PROOF_REPLACE_OUTPUT=1` is required to overwrite a non-empty generated proof root.
21. `scripts/macos-provider-deployment-evidence.py` - collects and verifies raw QA-host deployment evidence for the signed/notarized app bundle, expected Team ID, exact app and system extension IDs, activated/enabled `systemextensionsctl` state, strict app, deep app-bundle, and embedded-system-extension code-signature verification, required app/system-extension entitlements, embedded provider path, app/extension plist identifiers, and EndpointSecurity plus NetworkExtension extension points.
22. `scripts/macos-provider-dogfood-manifest.py` - writes and verifies the combined macOS provider evidence manifest with SHA-256 hashes for selected summaries plus every raw artifact under the run output root, requires selected artifact paths to remain inside the run root, requires a non-empty absolute recorded `runRoot` while allowing relocated verification with an explicit warning, rejects boolean `schemaVersion`, boolean/non-integer manifest inventory counts, selected-artifact byte fields, saved-gate counters, numeric provider-health flags, non-boolean embedded verifier `verified` fields, and non-string deployment extension-point bindings, enforces closed schemas for the manifest envelope, selected artifact records, artifact inventory envelope, inventory file rows, saved gate-result envelope, provider bindings, ES/NE provider-health records, and embedded deployment/ES/NE verifier outputs, records `evidenceMode` (`live` for production readiness, `fixture` for self-tests), requires manifest run/target metadata to match the saved gate, requires the saved `gate-result.json` to bind the selected summary artifacts and provider-health bindings, compares its portable decision projection with a fresh gate, and reruns the combined deployment/ES/NE gate during manifest verification.
23. `scripts/endpoint-security-live-dogfood-verify.py` - validates an EndpointSecurity dogfood `summary.json` rejects synthetic ingest, missing health/graph/receipt/probe/protection-state artifact files, missing graph proof, missing receipt proof, unsigned sensor-state proof metadata, uninstalled/unapproved/inactive/degraded macOS provider attestation, wrong provider IDs, malformed probe paths, mismatched probe-target hashes, mismatched host/user/agent context, and invalid managed-observer artifact metadata.
24. `scripts/network-extension-live-dogfood-verify.py` - validates a NetworkExtension containment `summary.json` rejects missing health/findings/action/proof/flow/rollback artifact files, weak proof, summary/proof drift, missing provider reload delivery, missing provider runtime/sensor-state proof metadata, uninstalled/unapproved/inactive/degraded macOS provider attestation, non-blocked final flow, skipped/failed rollback, failed post-rollback reachability, mismatched host/user/agent context, and dropped verdicts.
25. `scripts/macos-provider-dogfood-gate.py` - combines verified deployment evidence, EndpointSecurity, and NetworkExtension `summary.json` artifacts into one deployed-provider go/no-go gate, requiring the summaries to come from the same host/user context with the same run ID inside a bounded run window and binding deployment extension points to ES/NE provider-health attestations.
26. `scripts/endpoint-decision-engine-readiness-audit.py` - machine-readable completion audit that maps the north-star EDR requirements to concrete evidence, accepts a macOS provider dogfood `manifest.json`, verifies the manifest/fresh gate/provider bindings, requires `evidenceMode=live` on the macOS provider and supplemental source manifests before production readiness can pass, supports repeated `--proof KEY=PATH` supplemental proof artifacts for non-macOS checklist items, rejects bare proxy claims by requiring key-specific evidence fields plus SHA-256/byte-bound artifact and command-output records, rejects hidden supplemental proof fields by enforcing closed proof envelope, evidence, artifact, and command-record schemas with non-boolean integer byte/exit-code checks plus boolean `schemaVersion` rejection, prints validator-backed evidence and command-result skeletons with `--proof-template KEY|all`, can generate strict supplemental proof JSON files from operator evidence with `--write-proof KEY --proof-output PATH --proof-evidence PATH --proof-artifact PATH --proof-command-result PATH`, and can persist or verify self-hashed audit reports with `--output` / `--verify-audit`; persisted audits reject hidden audit envelope, provenance, checklist, manifest-verification, and supplemental-source provenance fields, boolean `schemaVersion`, boolean/non-integer audit counts, manifest-verification counters, checklist evidence/gap rows, and supplemental-source provenance counts, and include provenance paths so verification also re-checks the recorded manifest, supplemental proofs, staged supplemental source manifest, source-manifest boolean `schemaVersion`, source-manifest `verified` status, proofRoot/sourceRoot topology, generated-proof/source/bridge record counts, strict non-boolean byte-size records, closed source-manifest envelope, policy, sourceArtifacts, and `path`/`sha256`/`byteSize` record schemas, source artifact containment under sourceRoot unless explicitly disclosed as external qualification evidence, exact external-source disclosure maps, expected bridge-script paths, source policy metadata, and local proposed-policy file hash binding when available, and a complete supplemental proof set must include `--supplemental-source-manifest PATH` or the prove-later criterion stays failed.
27. `scripts/endpoint-decision-engine-qualification-bundle.py` - verifies a complete Endpoint Decision Engine qualification evidence bundle by discovering the macOS provider dogfood manifest and all strict supplemental proof JSON files, running the readiness audit, writing the persisted self-hashed audit, and immediately re-verifying that persisted audit against the recorded source manifest/proofs; a complete supplemental proof set must include `supplemental-proof-source-manifest.json` with `evidenceMode=live`, generated-proof, staged-source, and bridge-script SHA-256/size records, valid proofRoot/sourceRoot topology, strict non-boolean byte-size records, closed source-manifest envelope, policy, sourceArtifacts, and `path`/`sha256`/`byteSize` record schemas, valid source policy metadata whose local proposed-policy ref hash matches the file when it resolves locally, manifest, proof-root, explicit proof paths, supplemental source manifest, and persisted audit output must live inside `--bundle-dir` unless `--allow-external-evidence` or `--allow-external-output` is passed, non-external source records must live under the declared sourceRoot, generated-proof records must match the proof paths being qualified, external evidence/output paths and optional bounded machine-safe `--metadata KEY=VALUE` driver/operator metadata are disclosed in a self-hashed summary that can be rechecked with `--verify-summary PATH`, and that verifier rejects hidden qualification-summary and supplemental-source-summary fields plus boolean summary/source-manifest `schemaVersion`, boolean/non-integer summary count, and supplemental-source count/verified coercions while cross-checking summary audit hash, counts, failed/unresolved/missing keys, persisted-verification flags and payload, external evidence/output opt-in flags, bundle directory, manifest path, proof paths, source-manifest `verified`, `evidenceMode`, and metadata, qualification metadata, expected bridge-script paths, and external evidence/output disclosures against the persisted audit and live source manifest; its self-test proves fixture bundles are replayable but not production-ready, summary mutation, hash-rebound summary drift, boolean summary-count and source-manifest verified/count/schema rejection, summary-underlying-audit drift, persisted-verification payload drift, source verification/evidence-mode drift, external trust-boundary flag drift, hidden summary/metadata/disclosure rejection, bundle-boundary drift rejection, missing-source-manifest, invalid byte-size schema, unsupported source-manifest fields, missing/wrong generated-proof or bridge-script record, invalid topology, invalid or mismatched source policy metadata, missing-proof, duplicate-proof, staged-source mutation, metadata reporting, invalid-metadata rejection, output-overwrite rejection, and external opt-in behavior.
28. `scripts/endpoint-decision-engine-supplemental-proof-bundle.py` - builds the staged supplemental proof root expected by live qualification from explicit source coverage artifacts, copies those inputs into `source-artifacts/`, writes relocatable `supplemental-proof-source-manifest.json` with `evidenceMode=live` by default, proofRoot/sourceRoot topology, SHA-256/size records for generated proofs, staged sources, and bridge scripts plus validated current/proposed policy refs, proposed policy hash, and policy epoch, omits unverified origin `sourcePath` metadata from the manifest so later relocation checks trust only staged artifact hashes, rejects a proposed policy hash that does not match a local proposed-policy ref, runs every strict proof bridge, and rejects missing or duplicate generated proof keys; normal builds refuse a non-empty `--out-dir` unless `--replace-output` is passed, and `--preflight` validates source artifact readability and output path safety, then dry-runs the same bridge pipeline in a temporary directory without creating or replacing the requested proof root.
29. `scripts/policy-simulation-impact-proof.py` - converts `clawdstrike policy impact --json` output into the stricter `policy_simulation_impact` supplemental proof path by requiring replayed event history, a bounded history window, audit-mode and staged-enforcement support, breakage-driver records, and a `simulation` receipt object, then hashing the event stream, impact result, breakage drivers, and receipt before delegating final proof JSON generation to `scripts/endpoint-decision-engine-readiness-audit.py --write-proof`.
30. `scripts/ai-agent-developer-workstation-proof.py` - derives the stricter `ai_agent_developer_workstation` supplemental proof from a structured local EDR coverage artifact, requiring MCP/browser/shell/package-manager/cloud-CLI runtime rows, protected-surface coverage for local API keys, repo secrets, CI tokens, browser cookies, package-registry tokens, cloud credentials, and prompt-injected tool execution, identity-field coverage, collector-kind coverage, raw-secret omission evidence, receipt-to-activity bindings, and non-isolated causal graph activity nodes before delegating final proof JSON generation to the readiness audit writer.
31. `scripts/endpoint-deception-proof.py` - derives the stricter `endpoint_deception` supplemental proof from a structured deception coverage artifact, requiring materialization and detection receipt IDs, the `deception.honey_artifact_touched` rule, causal graph slice evidence, receipt bindings from materialization to honey artifacts and detection to touched artifacts/graph, process-to-honey graph edges, SHA-256-bound materialization/detection/graph payloads, materialized and touched honey artifact counts, and file/SSH-key/browser-cookie/API-token/hostname honey-kind coverage before delegating final proof JSON generation to the readiness audit writer.
32. `scripts/supply-chain-runtime-guard-proof.py` - derives the stricter `supply_chain_runtime_guard` supplemental proof from structured supply-chain coverage rows, requiring npm/pip/Cargo package-manager coverage, package-script, unsigned-binary, signature-drift, dynamic-library-injection, launch-persistence, browser-extension, and developer-tool surfaces, the actual `supply_chain.*` guard rule IDs, SHA-256-bound evidence slices, positive observation counts, receipt-to-observation bindings, and non-isolated graph nodes for every observation before delegating final proof JSON generation to the readiness audit writer.
33. `scripts/privacy-preserving-telemetry-proof.py` - derives the stricter `privacy_preserving_telemetry` supplemental proof from privacy-report response artifacts, requiring non-raw default projection, raw-artifact suppression by default, downgrade behavior for unapproved raw requests, policy-plus-approval-gated raw artifact upload, signed `privacy_report` receipt evidence that binds the report and approval, SHA-256-bound report/policy/receipt artifacts, required projection classes, positive counts, and raw values only in the approved report before delegating final proof JSON generation to the readiness audit writer.
34. `scripts/operator-workflows-proof.py` - derives the stricter `operator_workflows` supplemental proof from operator workflow dogfood/export artifacts, requiring every named workflow, concrete local EDR API route coverage, simulation/response/sensor/privacy receipt-family coverage, verified workflow runs with latency in the 10-second north-star bound, local-first evidence that decisions and containment still work when cloud/NATS are unavailable, bounded containment TTL plus rollback evidence, full controlled-response action coverage for isolate-network, suspend-process-tree, revoke-token, quarantine-file, block-persistence, rollback-config, and collect-evidence actions with per-action TTL, rollback, and receipt proof that binds policy, sensor state, actor, process tree, evidence, confidence, and action, staged-detection generation, and SHA-256-bound operator export/proof-package artifacts before delegating final proof JSON generation to the readiness audit writer.
35. `scripts/cross-platform-sensor-breadth-proof.py` - derives the stricter `cross_platform_sensor_breadth` supplemental proof from sensor inventory, event coverage, route coverage, redaction, and graph-persistence artifacts, requiring process/file/network/DNS/persistence/identity/browser/package-manager/secrets coverage across macOS/Linux/Windows, required event kinds including process exec, file access/write, network flow, DNS lookup, launch persistence, identity context, browser download, package script, shell command, tool call, credential access, and policy decision events, local EDR ingestion routes, identity fields, redaction coverage, graph persistence with required causal node and edge kinds plus upstream/downstream/process-tree query coverage, and SHA-256-bound coverage slices before delegating final proof JSON generation to the readiness audit writer.
36. `scripts/test-macos-provider-dogfood-contract.sh` - no-live local contract gate for the macOS provider dogfood path; runs shell syntax checks, Python compile checks, explicit live `evidenceMode` wiring checks, live qualification fail-closed logging checks, wrapper preflight self-test, ES/NE/deployment/gate/manifest verifier self-tests, the readiness-audit self-test, qualification-bundle self-test, supplemental-proof-bundle self-test, and all supplemental proof bridge self-tests.
