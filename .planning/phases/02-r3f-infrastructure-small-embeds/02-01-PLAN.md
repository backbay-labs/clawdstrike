---
phase: 02-r3f-infrastructure-small-embeds
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/workbench/package.json
  - apps/workbench/src/features/spirit/types.ts
  - apps/workbench/src/features/spirit/stores/spirit-store.ts
  - apps/workbench/src/features/spirit/__tests__/spirit-orb-icon.test.tsx
  - apps/workbench/src/features/spirit/__tests__/spirit-companion-canvas.test.tsx
  - apps/workbench/src/features/spirit/__tests__/spirit-chamber-tab.test.tsx
  - apps/workbench/src/features/activity-bar/__tests__/activity-bar-item.test.tsx
  - apps/workbench/src/features/right-sidebar/__tests__/right-sidebar-store.test.ts
autonomous: true
requirements:
  - SPRT-03
  - SPRT-04
  - SPRT-05

must_haves:
  truths:
    - "R3F packages (three, @react-three/fiber, @react-three/drei, @types/three) are installable and importable without peer-dep errors"
    - "SpiritKind is sentinel | oracle | witness | specter (huntronomer names, not ember/tide/verdant/void/neutral)"
    - "SPIRIT_ACCENT_MAP keys match the new SpiritKind union; TypeScript reports zero errors"
    - "All five Wave 0 test scaffold files exist with failing tests that define the expected contracts"
  artifacts:
    - path: "apps/workbench/package.json"
      provides: "three, @react-three/fiber, @react-three/drei runtime deps; @types/three dev dep"
    - path: "apps/workbench/src/features/spirit/types.ts"
      provides: "SpiritKind = sentinel | oracle | witness | specter"
    - path: "apps/workbench/src/features/spirit/stores/spirit-store.ts"
      provides: "SPIRIT_ACCENT_MAP with huntronomer hex colors"
    - path: "apps/workbench/src/features/spirit/__tests__/spirit-orb-icon.test.tsx"
      provides: "failing tests for SpiritOrbIcon component contracts"
    - path: "apps/workbench/src/features/spirit/__tests__/spirit-companion-canvas.test.tsx"
      provides: "failing tests for SpiritCompanionCanvas null-guard and render contracts"
    - path: "apps/workbench/src/features/spirit/__tests__/spirit-chamber-tab.test.tsx"
      provides: "failing tests for SpiritChamberTab bind/unbind form contracts"
    - path: "apps/workbench/src/features/activity-bar/__tests__/activity-bar-item.test.tsx"
      provides: "failing tests for ActivityBarItem orbColor prop"
    - path: "apps/workbench/src/features/right-sidebar/__tests__/right-sidebar-store.test.ts"
      provides: "failing tests for RightSidebarPanel type extension"
  key_links:
    - from: "spirit-store.ts SPIRIT_ACCENT_MAP"
      to: "types.ts SpiritKind"
      via: "Record<SpiritKind, string>"
      pattern: "Record<SpiritKind"
---

<objective>
Install R3F packages, migrate SpiritKind from placeholder names to huntronomer names, and create Wave 0 test scaffolds for all Phase 2 behavioral contracts.

Purpose: Establishes the dependency foundation and type system before any component work begins. The SpiritKind migration is done first so that TypeScript catches every reference that needs updating — any component referencing the old kind strings ("ember", "tide") will break with a compile error, providing a full audit trail.

Output: Installed R3F packages, updated SpiritKind + SPIRIT_ACCENT_MAP, five failing test files that define the expected behavior contracts for Wave 2 tasks.
</objective>

<execution_context>
@/Users/connor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-r3f-infrastructure-small-embeds/02-CONTEXT.md
@.planning/phases/02-r3f-infrastructure-small-embeds/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From apps/workbench/src/features/spirit/types.ts (CURRENT — will be replaced):
```typescript
export type SpiritKind =
  | "ember"     // REPLACE with huntronomer kinds
  | "tide"
  | "verdant"
  | "void"
  | "neutral";

export type SpiritMood = "idle" | "active" | "alert" | "dormant";

export interface SpiritState {
  kind: SpiritKind | null;
  mood: SpiritMood;
  fieldStrength: number;
  accentColor: string | null;
  actions: {
    bindSpirit: (kind: SpiritKind) => void;
    unbindSpirit: () => void;
    setMood: (mood: SpiritMood) => void;
    setFieldStrength: (strength: number) => void;
  };
}
```

From apps/workbench/src/features/spirit/stores/spirit-store.ts (CURRENT — SPIRIT_ACCENT_MAP will change):
```typescript
const SPIRIT_ACCENT_MAP: Record<SpiritKind, string> = {
  ember: "#e05c3a",
  tide: "#3a8ae0",
  verdant: "#3dbf84",
  void: "#7a3ae0",
  neutral: "#d4a84b",
};
```

From apps/workbench/src/features/right-sidebar/types.ts:
```typescript
export type RightSidebarPanel = "speakeasy";
// Will be extended to: "speakeasy" | "spirit"
```

From apps/workbench/src/features/activity-bar/components/activity-bar-item.tsx:
```typescript
interface ActivityBarItemProps {
  id: ActivityBarItemId;
  icon: ComponentType<SigilProps>;
  tooltip: string;
  active: boolean;
  onClick: () => void;
  badge?: number;
  // orbColor?: string  — WILL BE ADDED in 02-02
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install R3F packages + migrate SpiritKind to huntronomer names</name>
  <files>
    apps/workbench/package.json
    apps/workbench/src/features/spirit/types.ts
    apps/workbench/src/features/spirit/stores/spirit-store.ts
  </files>
  <read_first>
    - apps/workbench/package.json — to see current deps before adding new ones
    - apps/workbench/src/features/spirit/types.ts — current SpiritKind to replace
    - apps/workbench/src/features/spirit/stores/spirit-store.ts — SPIRIT_ACCENT_MAP to replace
  </read_first>
  <behavior>
    - After SpiritKind update, TypeScript compile shows zero errors on spirit-store.ts itself
    - bindSpirit("sentinel") resolves correctly via SPIRIT_ACCENT_MAP["sentinel"] = "#3dbf84"
    - bindSpirit("ember") would be a TypeScript type error (old kind string is gone)
  </behavior>
  <action>
Step 1 — Install R3F packages in apps/workbench:

```bash
cd /Users/connor/Medica/backbay/standalone/clawdstrike-worktrees/huntronomer-workbench/apps/workbench
bun add three @react-three/fiber @react-three/drei
bun add -d @types/three
```

Pin `@react-three/fiber` as `"^9.0.0"` (huntronomer uses this pin; R3F 9.5.0 has peerDep `react: '>=19 <19.3'` and the workbench is on React 19.2.4 — do NOT use `"^9.5.0"`). After bun add, manually verify the version written to package.json is `"^9.0.0"` for fiber. If bun writes `"^9.5.0"`, edit package.json to `"^9.0.0"` then run `bun install` again.

Expected versions to land in package.json:
- `"three": "^0.170.0"` (resolves to 0.183.2 — compatible)
- `"@react-three/fiber": "^9.0.0"` (resolves to 9.5.0)
- `"@react-three/drei": "^10.7.7"`
- `"@types/three": "^0.183.1"` (devDependencies)

Step 2 — Replace SpiritKind in types.ts:

Replace the entire SpiritKind type with the huntronomer names. The SpiritMood and SpiritState interfaces stay unchanged:

```typescript
export type SpiritKind = "sentinel" | "oracle" | "witness" | "specter";
```

Remove all the inline comments about ember/tide/verdant/void/neutral.

Step 3 — Replace SPIRIT_ACCENT_MAP in spirit-store.ts:

Replace the map with huntronomer hex colors:

```typescript
const SPIRIT_ACCENT_MAP: Record<SpiritKind, string> = {
  sentinel: "#3dbf84",  // green
  oracle:   "#7b68ee",  // medium purple
  witness:  "#d4a84b",  // amber/gold
  specter:  "#c45c5c",  // muted red
};
```

Step 4 — Fix any TypeScript errors. Run:
```bash
cd /Users/connor/Medica/backbay/standalone/clawdstrike-worktrees/huntronomer-workbench/apps/workbench
bun run typecheck 2>&1 | grep -i "spirit\|SpiritKind\|ember\|tide\|verdant\|void\|neutral" | head -30
```

If any files reference the old kind strings ("ember", "tide", "verdant", "void", "neutral") as SpiritKind values, update them to use the new kinds (likely no other files reference specific kind strings — only spirit-store.ts held the accent map). Any test files referencing old kind strings must be updated too.
  </action>
  <verify>
    <automated>
      cd /Users/connor/Medica/backbay/standalone/clawdstrike-worktrees/huntronomer-workbench/apps/workbench && bun run typecheck 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - grep -n '"three"' apps/workbench/package.json outputs a line with "^0.170"
    - grep -n 'react-three/fiber' apps/workbench/package.json outputs a line with "^9.0.0"
    - grep -n 'react-three/drei' apps/workbench/package.json outputs a line with "^10"
    - grep -n '@types/three' apps/workbench/package.json outputs a line in devDependencies
    - grep -n 'SpiritKind' apps/workbench/src/features/spirit/types.ts outputs exactly: "sentinel" | "oracle" | "witness" | "specter"
    - grep -n 'ember\|tide\|verdant\|void\|neutral' apps/workbench/src/features/spirit/types.ts outputs nothing
    - grep -n 'sentinel.*3dbf84' apps/workbench/src/features/spirit/stores/spirit-store.ts outputs a match
    - bun run typecheck exits 0 (zero TypeScript errors)
  </acceptance_criteria>
  <done>R3F packages installed, SpiritKind updated to sentinel/oracle/witness/specter, SPIRIT_ACCENT_MAP updated with huntronomer colors, TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create Wave 0 test scaffolds (all failing)</name>
  <files>
    apps/workbench/src/features/spirit/__tests__/spirit-orb-icon.test.tsx
    apps/workbench/src/features/spirit/__tests__/spirit-companion-canvas.test.tsx
    apps/workbench/src/features/spirit/__tests__/spirit-chamber-tab.test.tsx
    apps/workbench/src/features/activity-bar/__tests__/activity-bar-item.test.tsx
    apps/workbench/src/features/right-sidebar/__tests__/right-sidebar-store.test.ts
  </files>
  <read_first>
    - apps/workbench/src/test/setup.ts — understand existing test setup and imports
    - apps/workbench/vitest.config.ts — confirm glob pattern for test file discovery
    - apps/workbench/src/features/activity-bar/components/activity-bar-item.tsx — read existing props before writing tests for new prop
    - apps/workbench/src/features/right-sidebar/stores/right-sidebar-store.ts — read existing store before writing test
  </read_first>
  <action>
Create __tests__ directories if they do not exist:
```bash
mkdir -p /Users/connor/Medica/backbay/standalone/clawdstrike-worktrees/huntronomer-workbench/apps/workbench/src/features/spirit/__tests__
mkdir -p /Users/connor/Medica/backbay/standalone/clawdstrike-worktrees/huntronomer-workbench/apps/workbench/src/features/activity-bar/__tests__
mkdir -p /Users/connor/Medica/backbay/standalone/clawdstrike-worktrees/huntronomer-workbench/apps/workbench/src/features/right-sidebar/__tests__
```

**File 1: spirit-orb-icon.test.tsx**

The component does not exist yet — tests will fail with module-not-found. That is correct (Wave 0 = RED).

```tsx
// apps/workbench/src/features/spirit/__tests__/spirit-orb-icon.test.tsx
import { describe, it, expect } from "vitest";
import { render, screen } from "@testing-library/react";
import { SpiritOrbIcon } from "../components/spirit-orb-icon";

describe("SpiritOrbIcon", () => {
  it("renders a span with aria-hidden", () => {
    const { container } = render(<SpiritOrbIcon accentColor="#3dbf84" />);
    const span = container.querySelector("span[aria-hidden]");
    expect(span).not.toBeNull();
  });

  it("applies radial-gradient using the accentColor", () => {
    const { container } = render(<SpiritOrbIcon accentColor="#3dbf84" />);
    const span = container.querySelector("span") as HTMLElement;
    expect(span.style.background).toContain("radial-gradient");
    expect(span.style.background).toContain("3dbf84");
  });

  it("uses default size 16 when size prop omitted", () => {
    const { container } = render(<SpiritOrbIcon accentColor="#7b68ee" />);
    const span = container.querySelector("span") as HTMLElement;
    expect(span.style.width).toBe("16px");
    expect(span.style.height).toBe("16px");
  });

  it("accepts custom size prop", () => {
    const { container } = render(<SpiritOrbIcon accentColor="#c45c5c" size={24} />);
    const span = container.querySelector("span") as HTMLElement;
    expect(span.style.width).toBe("24px");
    expect(span.style.height).toBe("24px");
  });
});
```

**File 2: spirit-companion-canvas.test.tsx**

Mock @react-three/fiber to prevent WebGL errors in jsdom.

```tsx
// apps/workbench/src/features/spirit/__tests__/spirit-companion-canvas.test.tsx
import { describe, it, expect, vi, beforeEach } from "vitest";
import { render } from "@testing-library/react";
import { SpiritCompanionCanvas } from "../components/spirit-companion-canvas";
import { useSpiritStore } from "../stores/spirit-store";

vi.mock("@react-three/fiber", () => ({
  Canvas: ({ children }: { children: React.ReactNode }) => (
    <div data-testid="r3f-canvas">{children}</div>
  ),
  useFrame: vi.fn(),
  useThree: () => ({ invalidate: vi.fn() }),
}));

describe("SpiritCompanionCanvas", () => {
  beforeEach(() => {
    useSpiritStore.getState().actions.unbindSpirit();
  });

  it("renders null when no spirit is bound (accentColor is null)", () => {
    const { container } = render(<SpiritCompanionCanvas />);
    expect(container.firstChild).toBeNull();
  });

  it("renders a 150x150 wrapper when a spirit is bound", () => {
    useSpiritStore.getState().actions.bindSpirit("sentinel");
    const { container } = render(<SpiritCompanionCanvas />);
    const wrapper = container.firstChild as HTMLElement;
    expect(wrapper).not.toBeNull();
    expect(wrapper.style.width).toBe("150px");
    expect(wrapper.style.height).toBe("150px");
  });

  it("renders the R3F Canvas when a spirit is bound", () => {
    useSpiritStore.getState().actions.bindSpirit("oracle");
    const { getByTestId } = render(<SpiritCompanionCanvas />);
    expect(getByTestId("r3f-canvas")).toBeDefined();
  });
});
```

**File 3: spirit-chamber-tab.test.tsx**

```tsx
// apps/workbench/src/features/spirit/__tests__/spirit-chamber-tab.test.tsx
import { describe, it, expect, beforeEach } from "vitest";
import { render, screen, fireEvent } from "@testing-library/react";
import { SpiritChamberTab } from "../components/spirit-chamber-tab";
import { useSpiritStore } from "../stores/spirit-store";

describe("SpiritChamberTab", () => {
  beforeEach(() => {
    useSpiritStore.getState().actions.unbindSpirit();
  });

  it("renders a kind selector with exactly 4 options (sentinel/oracle/witness/specter)", () => {
    render(<SpiritChamberTab />);
    const options = screen.getAllByRole("option");
    const kinds = options.map((o) => o.getAttribute("value") ?? o.textContent?.toLowerCase());
    expect(kinds).toContain("sentinel");
    expect(kinds).toContain("oracle");
    expect(kinds).toContain("witness");
    expect(kinds).toContain("specter");
    // Exactly 4 spirit kinds (placeholder option may make total 5 — allow for it)
    const kindOptions = options.filter((o) => ["sentinel","oracle","witness","specter"].includes(o.getAttribute("value") ?? ""));
    expect(kindOptions).toHaveLength(4);
  });

  it("calls bindSpirit with selected kind when Bind button is clicked", () => {
    render(<SpiritChamberTab />);
    // Select oracle
    const select = screen.getByRole("combobox");
    fireEvent.change(select, { target: { value: "oracle" } });
    const bindBtn = screen.getByRole("button", { name: /bind/i });
    fireEvent.click(bindBtn);
    expect(useSpiritStore.getState().kind).toBe("oracle");
  });

  it("calls unbindSpirit when Unbind button is clicked and spirit is bound", () => {
    useSpiritStore.getState().actions.bindSpirit("witness");
    render(<SpiritChamberTab />);
    const unbindBtn = screen.getByRole("button", { name: /unbind/i });
    fireEvent.click(unbindBtn);
    expect(useSpiritStore.getState().kind).toBeNull();
  });

  it("shows current spirit kind when a spirit is bound", () => {
    useSpiritStore.getState().actions.bindSpirit("specter");
    render(<SpiritChamberTab />);
    expect(screen.getByText(/specter/i)).toBeDefined();
  });

  it("shows no-spirit placeholder text when no spirit is bound", () => {
    render(<SpiritChamberTab />);
    expect(screen.getByText(/no spirit/i)).toBeDefined();
  });
});
```

**File 4: activity-bar-item.test.tsx**

```tsx
// apps/workbench/src/features/activity-bar/__tests__/activity-bar-item.test.tsx
import { describe, it, expect, vi } from "vitest";
import { render, screen } from "@testing-library/react";
import { ActivityBarItem } from "../components/activity-bar-item";
import { SigilHunt } from "@/components/desktop/sidebar-icons";

describe("ActivityBarItem", () => {
  it("renders the icon when orbColor is not set", () => {
    render(
      <ActivityBarItem
        id="hunt"
        icon={SigilHunt}
        tooltip="Hunt"
        active={false}
        onClick={vi.fn()}
      />
    );
    // Icon renders without orb — no spirit-orb-icon present
    expect(screen.queryByRole("presentation")).toBeNull();
  });

  it("renders SpiritOrbIcon instead of icon when orbColor is provided", () => {
    const { container } = render(
      <ActivityBarItem
        id="hunt"
        icon={SigilHunt}
        tooltip="Hunt"
        active={false}
        onClick={vi.fn()}
        orbColor="#3dbf84"
      />
    );
    // SpiritOrbIcon renders a span with aria-hidden and radial-gradient
    const orb = container.querySelector("span[aria-hidden]");
    expect(orb).not.toBeNull();
  });
});
```

**File 5: right-sidebar-store.test.ts**

```typescript
// apps/workbench/src/features/right-sidebar/__tests__/right-sidebar-store.test.ts
import { describe, it, expect, beforeEach } from "vitest";
import { useRightSidebarStore } from "../stores/right-sidebar-store";

describe("RightSidebarStore — spirit panel support", () => {
  beforeEach(() => {
    // Reset to default state
    useRightSidebarStore.getState().actions.setActivePanel("speakeasy");
    useRightSidebarStore.getState().actions.hide();
  });

  it("accepts 'spirit' as a valid panel value for setActivePanel", () => {
    // This test FAILS until RightSidebarPanel type includes "spirit"
    useRightSidebarStore.getState().actions.setActivePanel("spirit");
    expect(useRightSidebarStore.getState().activePanel).toBe("spirit");
  });

  it("switches back from spirit to speakeasy", () => {
    useRightSidebarStore.getState().actions.setActivePanel("spirit");
    useRightSidebarStore.getState().actions.setActivePanel("speakeasy");
    expect(useRightSidebarStore.getState().activePanel).toBe("speakeasy");
  });
});
```

After creating all files, run the test suite to confirm the scaffolds load (they will fail on missing imports — that is correct):

```bash
cd /Users/connor/Medica/backbay/standalone/clawdstrike-worktrees/huntronomer-workbench/apps/workbench
bun run test --run 2>&1 | tail -20
```

Expected output: tests fail because `spirit-orb-icon`, `spirit-companion-canvas`, `spirit-chamber-tab` modules do not exist yet. The right-sidebar-store test should pass on the first assertion (setActivePanel will reject "spirit" with a TypeScript error — but the test runtime might still run). The important thing is that all 5 test files are discovered and produce failures, not "file not found" process errors.
  </action>
  <verify>
    <automated>
      cd /Users/connor/Medica/backbay/standalone/clawdstrike-worktrees/huntronomer-workbench/apps/workbench && bun run test --run 2>&1 | grep -E "spirit-orb-icon|spirit-companion|spirit-chamber|activity-bar-item|right-sidebar-store" | head -10
    </automated>
  </verify>
  <acceptance_criteria>
    - grep -rn "SpiritOrbIcon" apps/workbench/src/features/spirit/__tests__/spirit-orb-icon.test.tsx returns match
    - grep -rn "vi.mock.*react-three/fiber" apps/workbench/src/features/spirit/__tests__/spirit-companion-canvas.test.tsx returns match
    - grep -rn "sentinel.*oracle.*witness.*specter" apps/workbench/src/features/spirit/__tests__/spirit-chamber-tab.test.tsx returns match
    - grep -rn "orbColor" apps/workbench/src/features/activity-bar/__tests__/activity-bar-item.test.tsx returns match
    - grep -rn "setActivePanel.*spirit" apps/workbench/src/features/right-sidebar/__tests__/right-sidebar-store.test.ts returns match
    - bun run test --run 2>&1 output shows all 5 test files were discovered (run, even if failing)
  </acceptance_criteria>
  <done>Five Wave 0 test scaffold files created. Tests fail on missing module imports (spirit-orb-icon, spirit-companion-canvas, spirit-chamber-tab) or missing prop/type (orbColor, "spirit" panel). The failures define the exact contracts that Wave 2 plans must satisfy.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `cd apps/workbench && bun run typecheck` — exits 0 (no errors on spirit/types changes)
2. `grep '"three"' apps/workbench/package.json` — shows `"^0.170"`
3. `grep 'sentinel.*3dbf84' apps/workbench/src/features/spirit/stores/spirit-store.ts` — matches
4. `bun run test --run 2>&1 | grep -c "spirit\|activity-bar-item\|right-sidebar-store"` — shows 5+ test files discovered
</verification>

<success_criteria>
- R3F packages in package.json: three@^0.170, @react-three/fiber@^9.0.0, @react-three/drei@^10, @types/three devDep
- SpiritKind = "sentinel" | "oracle" | "witness" | "specter" (no ember/tide/verdant/void/neutral)
- SPIRIT_ACCENT_MAP: sentinel=#3dbf84, oracle=#7b68ee, witness=#d4a84b, specter=#c45c5c
- TypeScript compiles clean (bun run typecheck exits 0)
- 5 test scaffold files exist and are discovered by vitest
</success_criteria>

<output>
After completion, create `.planning/phases/02-r3f-infrastructure-small-embeds/02-01-SUMMARY.md`
</output>
