---
phase: 03-full-immersive-panes-observatory-forensics
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/workbench/src/features/forensics/__tests__/forensics-tape-panel.test.tsx
  - apps/workbench/src/features/forensics/types.ts
  - apps/workbench/src/features/forensics/components/ForensicsTapePanel.tsx
  - apps/workbench/src/features/bottom-pane/bottom-pane-store.ts
  - apps/workbench/src/features/bottom-pane/bottom-pane.tsx
autonomous: true
requirements:
  - FRNX-01
user_setup: []

must_haves:
  truths:
    - "User sees a 'Tape' button in the bottom pane tab bar alongside Terminal, Problems, Output, Audit"
    - "Clicking Tape opens a horizontal scrolling CSS-only event timeline with 4 mock events (allow/deny/probe/receipt)"
    - "Each mock event card shows a colored indicator dot (allow=#3dbf84, deny=#c45c5c, probe=#7b68ee, receipt=#d4a84b), timestamp relative label, and event label text"
    - "The tape panel has overscroll-x-contain to prevent WebKit rubber-band bounce (pitfall 6)"
    - "A footer note reads 'forensics tape — mock data — live telemetry deferred'"
  artifacts:
    - path: "apps/workbench/src/features/forensics/types.ts"
      provides: "TapeEvent interface: { id, timestamp, kind, label, stationId? }"
      exports: ["TapeEvent"]
    - path: "apps/workbench/src/features/forensics/components/ForensicsTapePanel.tsx"
      provides: "CSS-only horizontal scrollable timeline of mock forensics events"
      exports: ["ForensicsTapePanel"]
    - path: "apps/workbench/src/features/bottom-pane/bottom-pane-store.ts"
      provides: "BottomPaneTab union extended with 'tape'"
      contains: "tape"
    - path: "apps/workbench/src/features/bottom-pane/bottom-pane.tsx"
      provides: "Tape button in header + ForensicsTapePanel render case"
      contains: "ForensicsTapePanel"
  key_links:
    - from: "apps/workbench/src/features/bottom-pane/bottom-pane.tsx"
      to: "apps/workbench/src/features/forensics/components/ForensicsTapePanel.tsx"
      via: "conditional render: activeTab === 'tape' && <ForensicsTapePanel />"
      pattern: "activeTab.*tape.*ForensicsTapePanel"
    - from: "apps/workbench/src/features/bottom-pane/bottom-pane-store.ts"
      to: "bottom-pane.tsx"
      via: "BottomPaneTab type union includes 'tape'"
      pattern: "\"tape\""
---

<objective>
Add the Forensics Tape tab to the bottom pane as a CSS-only horizontal scrolling event timeline. No R3F — glia-three is deferred. Registers alongside the existing Terminal/Problems/Output/Audit tabs.

Purpose: Delivers FRNX-01 — forensics river mini-view in bottom pane "Tape" tab.
Output: ForensicsTapePanel (CSS horizontal timeline), TapeEvent type, bottom-pane-store "tape" union extension, Tape button in bottom-pane.tsx. Fully independent of observatory plans — runs in Wave 1 in parallel with 03-01.
</objective>

<execution_context>
@/Users/connor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-full-immersive-panes-observatory-forensics/03-CONTEXT.md
@.planning/phases/03-full-immersive-panes-observatory-forensics/03-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 test scaffold + TapeEvent type + ForensicsTapePanel CSS component</name>
  <files>
    apps/workbench/src/features/forensics/__tests__/forensics-tape-panel.test.tsx
    apps/workbench/src/features/forensics/types.ts
    apps/workbench/src/features/forensics/components/ForensicsTapePanel.tsx
  </files>

  <read_first>
    - apps/workbench/src/features/bottom-pane/audit-tail-panel.tsx (pattern for bottom pane panel components)
    - apps/workbench/src/features/bottom-pane/bottom-pane-store.ts (BottomPaneTab type — verify current shape before extending)
    - .planning/phases/03-full-immersive-panes-observatory-forensics/03-RESEARCH.md (Pattern 4: bottom pane tape tab; pitfall 6: WebKit overscroll; ForensicsTapePanel CSS-only mock code example)
    - /Users/connor/Medica/backbay/standalone/clawdstrike-worktrees/huntronomer-workspace-orch/apps/desktop/src/features/forensics/ForensicsRiverView.tsx (read to confirm glia-three dependency — confirms we're NOT porting R3F version)
  </read_first>

  <behavior>
    forensics-tape-panel.test.tsx:
    - Test: ForensicsTapePanel renders without crash
    - Test: renders 4 mock event cards (MOCK_EVENTS has 4 entries — query by role or data-testid)
    - Test: "forensics tape — mock data" text appears in the footer
    - Test: each event card shows the event label text
    - Test: TapeEvent type includes id, timestamp, kind ("allow"|"deny"|"probe"|"receipt"), label

    ForensicsTapePanel:
    - Returns a flex column container: h-full min-h-0 flex-col
    - Event timeline row: flex-1 min-h-0 items-center gap-3 overflow-x-auto px-4 overscroll-x-contain
    - Footer: h-6 shrink-0 border-t border-[#202531] px-4 with "forensics tape — mock data — live telemetry deferred" in text-[9px] font-mono text-[#6f7f9a]/40
  </behavior>

  <action>
    **First, write the test scaffold** (RED): Create apps/workbench/src/features/forensics/__tests__/forensics-tape-panel.test.tsx. Import ForensicsTapePanel from "@/features/forensics/components/ForensicsTapePanel". Write 4 tests as described in behavior block. Run test — MUST fail (file doesn't exist yet).

    **Create types.ts**: apps/workbench/src/features/forensics/types.ts:
    ```typescript
    export type TapeEventKind = "allow" | "deny" | "probe" | "receipt";

    export interface TapeEvent {
      id: string;
      timestamp: number;
      kind: TapeEventKind;
      label: string;
      stationId?: string;
    }
    ```

    **Create ForensicsTapePanel.tsx**: Port the CSS-only mock from RESEARCH.md Pattern 4 code example exactly. Use the full implementation shown there:
    - MOCK_EVENTS array with 4 entries (allow/deny/probe/receipt kinds with realistic labels: "file_read /etc/hosts", "shell_exec rm -rf", "Ed25519 signed", "station:run scanned")
    - KIND_COLOR record: allow=#3dbf84, deny=#c45c5c, probe=#7b68ee, receipt=#d4a84b
    - TapeEventCard subcomponent: small card (width: 160px min-width), dark bg-[#0f1219], border border-[#202531], rounded-md, px-3 py-2. Top row: colored dot (width 7px, height 7px, rounded-full, bg from KIND_COLOR) + kind label (text-[9px] font-mono uppercase). Bottom: label text (text-[10px] text-[#ece7dc] truncate). Footer inside card: timestamp relative (Date.now() - timestamp displayed as "Xs ago").
    - Main container: `<div className="flex h-full min-h-0 flex-col">`
    - Timeline row: `<div className="flex flex-1 min-h-0 items-center gap-3 overflow-x-auto px-4 overscroll-x-contain" style={{ scrollbarWidth: "thin" }}>`
    - Footer row: `<div className="flex h-6 shrink-0 items-center border-t border-[#202531] px-4">` with the deferred notice text.

    CRITICAL per pitfall 6 (RESEARCH.md): `overscroll-x-contain` on the timeline row. This prevents WebKit rubber-band bounce propagating to the parent pane.

    Run `npm test --workspace=apps/workbench -- forensics-tape-panel --reporter=dot` — tests should pass GREEN. Commit: "feat(03-04): ForensicsTapePanel CSS-only mock timeline".
  </action>

  <verify>
    <automated>npm test --workspace=apps/workbench -- forensics-tape-panel --reporter=dot</automated>
  </verify>

  <acceptance_criteria>
    - grep "TapeEvent" apps/workbench/src/features/forensics/types.ts → interface exported
    - grep "allow.*deny.*probe.*receipt" apps/workbench/src/features/forensics/types.ts → TapeEventKind union present
    - grep "overscroll-x-contain" apps/workbench/src/features/forensics/components/ForensicsTapePanel.tsx → WebKit fix present
    - grep "forensics tape.*mock data" apps/workbench/src/features/forensics/components/ForensicsTapePanel.tsx → footer text present
    - grep "MOCK_EVENTS" apps/workbench/src/features/forensics/components/ForensicsTapePanel.tsx → mock data defined
    - grep "KIND_COLOR" apps/workbench/src/features/forensics/components/ForensicsTapePanel.tsx → color map present
    - npm test forensics-tape-panel → PASS (all 4 tests green)
  </acceptance_criteria>

  <done>ForensicsTapePanel renders 4 mock event cards in a horizontal scrollable row with overscroll protection. Footer shows deferred notice. Tests GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Extend BottomPaneTab union + add Tape button + render case in bottom-pane.tsx</name>
  <files>
    apps/workbench/src/features/bottom-pane/bottom-pane-store.ts
    apps/workbench/src/features/bottom-pane/bottom-pane.tsx
  </files>

  <read_first>
    - apps/workbench/src/features/bottom-pane/bottom-pane-store.ts (current BottomPaneTab = "terminal" | "problems" | "output" | "audit" — add "tape")
    - apps/workbench/src/features/bottom-pane/bottom-pane.tsx (current Audit button pattern — replicate exactly for Tape button)
    - apps/workbench/src/features/forensics/__tests__/forensics-tape-panel.test.tsx (extend with bottom-pane integration test: Tape button visible, clicking opens ForensicsTapePanel)
    - .planning/phases/03-full-immersive-panes-observatory-forensics/03-RESEARCH.md (Pattern 4: bottom pane tape tab — exact button + render case code)
  </read_first>

  <action>
    **bottom-pane-store.ts**: Change the BottomPaneTab type union to include "tape":
    ```typescript
    export type BottomPaneTab = "terminal" | "problems" | "output" | "audit" | "tape";
    ```
    No other changes to the store — toggleTab, setActiveTab, all other actions already work with any BottomPaneTab value.

    **bottom-pane.tsx**: Follow the exact Audit button pattern:
    1. Add import: `import { ForensicsTapePanel } from "@/features/forensics/components/ForensicsTapePanel";`
    2. Add import for icon: `IconTimeline` from `@tabler/icons-react` (check if it exists; if not, use `IconList` or `IconActivity` — pick the closest available Tabler icon for a timeline/tape concept).
    3. Add Tape button after the Audit button (same CSS class pattern):
       ```tsx
       <button
         type="button"
         className={cn(
           "inline-flex items-center gap-1.5 rounded-md px-2.5 py-1 text-[11px] font-medium transition-colors",
           activeTab === "tape"
             ? "bg-[#131721] text-[#ece7dc]"
             : "text-[#6f7f9a] hover:bg-[#0f1219] hover:text-[#ece7dc]",
         )}
         onClick={() => useBottomPaneStore.getState().setActiveTab("tape")}
       >
         <IconTimeline size={13} stroke={1.8} />
         Tape
       </button>
       ```
    4. Add render case for tape tab. The content area render chain is a ternary — extend the last branch:
       Find the existing pattern: `activeTab === "audit" ? <AuditTailPanel /> : <OutputPanel />`
       Change to: `activeTab === "audit" ? <AuditTailPanel /> : activeTab === "tape" ? <ForensicsTapePanel /> : <OutputPanel />`

    Note: Check current tabler-icons-react for IconTimeline — if missing, use `IconActivityHeartbeat` or `IconTimeline` (it exists in newer Tabler versions). If unavailable, fall back to `IconList` with the text "Tape" making it self-explanatory.

    Run `npm test --workspace=apps/workbench -- --reporter=dot` — full suite must pass. Commit: "feat(03-04): add Tape tab to bottom pane (BottomPaneTab union + button + render case)".
  </action>

  <verify>
    <automated>npm test --workspace=apps/workbench -- --reporter=dot</automated>
  </verify>

  <acceptance_criteria>
    - grep "tape" apps/workbench/src/features/bottom-pane/bottom-pane-store.ts → "tape" in BottomPaneTab union
    - grep "ForensicsTapePanel" apps/workbench/src/features/bottom-pane/bottom-pane.tsx → imported and rendered
    - grep "Tape" apps/workbench/src/features/bottom-pane/bottom-pane.tsx → button label present
    - grep "setActiveTab.*tape\|activeTab.*tape" apps/workbench/src/features/bottom-pane/bottom-pane.tsx → click handler and render case present
    - grep "overscroll" apps/workbench/src/features/bottom-pane/bottom-pane.tsx → should NOT be needed here (already in ForensicsTapePanel)
    - npm test --workspace=apps/workbench → full suite PASS
  </acceptance_criteria>

  <done>Bottom pane tab bar shows Terminal | Problems | Output | Audit | Tape. Clicking Tape renders ForensicsTapePanel with horizontal mock event timeline. Full suite green.</done>
</task>

</tasks>

<verification>
Full suite: `npm test --workspace=apps/workbench`
Type check: TypeScript compiler accepts "tape" as BottomPaneTab in all call sites.
Manual: open workbench → click Tape in bottom pane → 4 mock event cards scroll horizontally.
</verification>

<success_criteria>
- BottomPaneTab union includes "tape"
- ForensicsTapePanel renders 4 mock events (allow/deny/probe/receipt) in CSS horizontal timeline
- Tape button visible in bottom pane header
- overscroll-x-contain prevents WebKit rubber-band bounce
- Footer shows deferred notice text
- No glia-three or R3F imports in forensics feature
- Full workbench test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/03-full-immersive-panes-observatory-forensics/03-04-SUMMARY.md`
</output>
