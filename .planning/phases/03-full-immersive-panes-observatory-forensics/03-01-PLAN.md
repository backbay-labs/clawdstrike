---
phase: 03-full-immersive-panes-observatory-forensics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/workbench/src/features/observatory/__tests__/observatory-tab.test.tsx
  - apps/workbench/src/features/observatory/__tests__/probe-runtime.test.ts
  - apps/workbench/src/features/observatory/__tests__/flow-mode-controller.test.tsx
  - apps/workbench/src/features/observatory/world/types.ts
  - apps/workbench/src/features/observatory/world/stations.ts
  - apps/workbench/src/features/observatory/world/deriveObservatoryWorld.ts
  - apps/workbench/src/features/observatory/components/ObservatoryWorldCanvas.tsx
  - apps/workbench/src/features/observatory/components/ObservatoryTab.tsx
  - apps/workbench/src/components/desktop/workbench-routes.tsx
autonomous: true
requirements:
  - OBS-03
user_setup: []

must_haves:
  truths:
    - "User can open /observatory route and see the 3D station sphere grid rendered in atlas mode filling the full pane"
    - "ObservatoryTab reads from useObservatoryStore.stations and useSpiritStore.accentColor — no hardcoded data"
    - "The WebGLSpikeCanvas placeholder is gone from workbench-routes.tsx"
    - "ObservatoryWorldCanvas renders 6 station spheres positioned via HUNT_STATION_PLACEMENTS, with drei Text labels above each"
  artifacts:
    - path: "apps/workbench/src/features/observatory/world/types.ts"
      provides: "HuntObservatoryMode, HuntStationId, HuntStationState, HuntObservatorySceneState — canonical types ObservatoryWorldCanvas accepts"
      exports: ["HuntObservatoryMode", "HuntStationId", "HuntStationState", "HuntObservatorySceneState"]
    - path: "apps/workbench/src/features/observatory/world/stations.ts"
      provides: "HUNT_STATION_PLACEMENTS, HUNT_STATION_LABELS, HUNT_STATION_ORDER constants"
      exports: ["HUNT_STATION_PLACEMENTS", "HUNT_STATION_LABELS"]
    - path: "apps/workbench/src/features/observatory/world/deriveObservatoryWorld.ts"
      provides: "pure function mapping mode+sceneState → DerivedObservatoryWorld"
      exports: ["deriveObservatoryWorld"]
    - path: "apps/workbench/src/features/observatory/components/ObservatoryWorldCanvas.tsx"
      provides: "R3F Canvas with station spheres, camera rig, lighting, Text labels"
      exports: ["ObservatoryWorldCanvas"]
    - path: "apps/workbench/src/features/observatory/components/ObservatoryTab.tsx"
      provides: "store bridge — reads workbench stores, builds HuntObservatorySceneState, renders ObservatoryWorldCanvas in absolute inset-0 wrapper"
      exports: ["ObservatoryTab"]
  key_links:
    - from: "apps/workbench/src/features/observatory/components/ObservatoryTab.tsx"
      to: "apps/workbench/src/features/observatory/stores/observatory-store.ts"
      via: "useObservatoryStore.use.stations()"
      pattern: "useObservatoryStore\\.use\\.stations"
    - from: "apps/workbench/src/features/observatory/components/ObservatoryTab.tsx"
      to: "apps/workbench/src/features/observatory/components/ObservatoryWorldCanvas.tsx"
      via: "JSX render"
      pattern: "ObservatoryWorldCanvas"
    - from: "apps/workbench/src/components/desktop/workbench-routes.tsx"
      to: "apps/workbench/src/features/observatory/components/ObservatoryTab.tsx"
      via: "React.lazy import at /observatory route"
      pattern: "ObservatoryTab"
---

<objective>
Port the observatory world foundation from huntronomer into the workbench as a full pane tab. Replaces the WebGLSpikeCanvas placeholder at /observatory with a real R3F scene rendering 6 station spheres in atlas mode.

Purpose: Delivers OBS-03 — the observatory world renders as a full editor pane tab accessible via command palette (observatory.open already registered as hunt.openObservatory).
Output: ObservatoryWorldCanvas (ported R3F scene), ObservatoryTab (store bridge wrapper), world types/constants/deriveObservatoryWorld (ported verbatim), Wave 0 test scaffolds.
</objective>

<execution_context>
@/Users/connor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-full-immersive-panes-observatory-forensics/03-CONTEXT.md
@.planning/phases/03-full-immersive-panes-observatory-forensics/03-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — test scaffolds for OBS-03, OBS-04, OBS-05, OBS-06</name>
  <files>
    apps/workbench/src/features/observatory/__tests__/observatory-tab.test.tsx
    apps/workbench/src/features/observatory/__tests__/probe-runtime.test.ts
    apps/workbench/src/features/observatory/__tests__/flow-mode-controller.test.tsx
  </files>

  <read_first>
    - /Users/connor/Medica/backbay/standalone/clawdstrike-worktrees/huntronomer-workspace-orch/apps/desktop/src/features/hunt-observatory/world/probeRuntime.test.ts (existing tests to adapt)
    - apps/workbench/src/features/spirit/__tests__/spirit-store.test.ts (existing test pattern for this codebase)
  </read_first>

  <behavior>
    observatory-tab.test.tsx (covers OBS-03 + OBS-05):
    - Test: ObservatoryTab renders without crash when no stations in store → renders a div wrapper (not null)
    - Test: ObservatoryTab renders without crash when 3 stations in store → renders ObservatoryWorldCanvas (mock Canvas returns null in jsdom)
    - Test: mode prop defaults to "atlas" on first render
    - Test: setMode("flow") changes mode state

    probe-runtime.test.ts (covers OBS-04):
    - Test: createInitialObservatoryProbeState() → status "ready", targetStationId null
    - Test: dispatchObservatoryProbe(ready, "signal", 0) → status "active", activeUntilMs = 5200
    - Test: advanceObservatoryProbeState(active at t=0, t=5200) → status "cooldown"
    - Test: advanceObservatoryProbeState(cooldown at t=5200, t=8800) → status "ready"
    - Test: canDispatchObservatoryProbe(active, t=100) → false
    - Test: getObservatoryProbeCharge(cooldown halfway) → ~0.5

    flow-mode-controller.test.tsx (covers OBS-06):
    - Test: FlowModeController renders null when characterControllerEnabled = false
    - Test: double-click event in flow mode sets characterControllerEnabled = true (use @ts-expect-error for the lazy component in jsdom)
  </behavior>

  <action>
    Create three test files in apps/workbench/src/features/observatory/__tests__/. The directory does not exist yet — create it.

    For observatory-tab.test.tsx: Mock @react-three/fiber Canvas as returning null (standard jsdom pattern used in this codebase — see spirit-companion-canvas tests from Phase 2). Mock useObservatoryStore and useSpiritStore. Tests are intentionally skeletal — they fail until Task 3 creates ObservatoryTab. Use @ts-expect-error where types don't exist yet (same pattern as Phase 2 wave 0 tests). Each test body: render the component, assert it doesn't throw.

    For probe-runtime.test.ts: Import from "@/features/observatory/world/probeRuntime" (does not exist yet — will fail until Task 2 creates it). Port the 6 test cases from huntronomer's probeRuntime.test.ts verbatim, adapting import path only.

    For flow-mode-controller.test.tsx: Import FlowModeController from "@/features/observatory/components/FlowModeController". Test that it renders null when disabled, and that the double-click handler flips characterControllerEnabled. Since FlowModeController doesn't exist yet, wrap imports in @ts-expect-error.

    Run `npm test --workspace=apps/workbench -- --reporter=dot` — tests MUST fail (RED) because implementation files don't exist yet. Confirm fail output and commit as "test(03-01): add Wave 0 scaffolds for observatory tests".
  </action>

  <verify>
    <automated>npm test --workspace=apps/workbench -- --reporter=dot 2>&1 | grep -E "FAIL|PASS|failed|passed"</automated>
  </verify>

  <acceptance_criteria>
    - grep -r "observatory-tab.test.tsx" apps/workbench/src/features/observatory/__tests__/ → file exists
    - grep -r "probe-runtime.test.ts" apps/workbench/src/features/observatory/__tests__/ → file exists
    - grep -r "flow-mode-controller.test.tsx" apps/workbench/src/features/observatory/__tests__/ → file exists
    - grep "createInitialObservatoryProbeState" apps/workbench/src/features/observatory/__tests__/probe-runtime.test.ts → test imports the function
    - Test suite reports 3 new failing test files (RED state confirmed)
  </acceptance_criteria>

  <done>Three test scaffold files exist. Tests are failing (RED) because implementation files don't exist yet.</done>
</task>

<task type="auto">
  <name>Task 2: Port world types, stations constants, deriveObservatoryWorld, probeRuntime</name>
  <files>
    apps/workbench/src/features/observatory/world/types.ts
    apps/workbench/src/features/observatory/world/stations.ts
    apps/workbench/src/features/observatory/world/deriveObservatoryWorld.ts
    apps/workbench/src/features/observatory/world/probeRuntime.ts
    apps/workbench/src/features/observatory/world/probeConsequences.ts
    apps/workbench/src/features/observatory/world/grounding.ts
  </files>

  <read_first>
    - /Users/connor/Medica/backbay/standalone/clawdstrike-worktrees/huntronomer-workspace-orch/apps/desktop/src/features/hunt-observatory/types.ts (port verbatim)
    - /Users/connor/Medica/backbay/standalone/clawdstrike-worktrees/huntronomer-workspace-orch/apps/desktop/src/features/hunt-observatory/stations.ts (port verbatim)
    - /Users/connor/Medica/backbay/standalone/clawdstrike-worktrees/huntronomer-workspace-orch/apps/desktop/src/features/hunt-observatory/world/deriveObservatoryWorld.ts (port verbatim)
    - /Users/connor/Medica/backbay/standalone/clawdstrike-worktrees/huntronomer-workspace-orch/apps/desktop/src/features/hunt-observatory/world/probeRuntime.ts (port verbatim — already read above)
    - /Users/connor/Medica/backbay/standalone/clawdstrike-worktrees/huntronomer-workspace-orch/apps/desktop/src/features/hunt-observatory/world/probeConsequences.ts (port verbatim)
    - /Users/connor/Medica/backbay/standalone/clawdstrike-worktrees/huntronomer-workspace-orch/apps/desktop/src/features/hunt-observatory/world/grounding.ts (port verbatim)
    - apps/workbench/src/features/observatory/types.ts (existing workbench types — do NOT modify; world/types.ts is separate)
  </read_first>

  <action>
    Port world types and pure functions from huntronomer verbatim. Create the world/ subdirectory inside apps/workbench/src/features/observatory/.

    **world/types.ts**: Copy huntronomer types.ts verbatim. These are the canonical types ObservatoryWorldCanvas accepts: HuntObservatoryMode, HuntStationId, HuntStationState, HuntObservatorySceneState, and all actor types. Note: these are DIFFERENT from apps/workbench/src/features/observatory/types.ts (workbench types) — coexist under different paths.

    **world/stations.ts**: Copy huntronomer stations.ts verbatim. Contains HUNT_STATION_PLACEMENTS (6 entries: signal/targets/run/receipts/case-notes/watch with angleDeg + radius), HUNT_STATION_LABELS, HUNT_STATION_ORDER. No external imports — pure constant data.

    **world/deriveObservatoryWorld.ts**: Copy huntronomer world/deriveObservatoryWorld.ts verbatim. Pure function — no context imports. If it imports from huntronomer-specific paths, remap to local world/types.ts and world/stations.ts only. Verify no OpenClaw, session, or mission imports exist in the source before porting.

    **world/probeRuntime.ts**: Copy huntronomer probeRuntime.ts verbatim (already read above — pure functions with zero external imports beyond HuntStationId from types.ts).

    **world/probeConsequences.ts**: Copy huntronomer probeConsequences.ts verbatim.

    **world/grounding.ts**: Copy huntronomer grounding.ts verbatim (terrain half-extents helpers).

    After creating files, run `npm test --workspace=apps/workbench -- probe-runtime --reporter=dot` — the probe-runtime.test.ts should now pass (GREEN). Commit: "feat(03-01): port observatory world types, stations, deriveObservatoryWorld, probeRuntime".
  </action>

  <verify>
    <automated>npm test --workspace=apps/workbench -- probe-runtime --reporter=dot</automated>
  </verify>

  <acceptance_criteria>
    - grep "HuntObservatorySceneState" apps/workbench/src/features/observatory/world/types.ts → type exported
    - grep "HUNT_STATION_PLACEMENTS" apps/workbench/src/features/observatory/world/stations.ts → constant exported
    - grep "deriveObservatoryWorld" apps/workbench/src/features/observatory/world/deriveObservatoryWorld.ts → function exported
    - grep "createInitialObservatoryProbeState" apps/workbench/src/features/observatory/world/probeRuntime.ts → function exported
    - npm test probe-runtime → PASS (all 6 tests green)
    - No imports of OpenClaw, session, or mission modules in any ported file
  </acceptance_criteria>

  <done>World pure-function layer ported. probe-runtime tests pass GREEN. deriveObservatoryWorld and constants available for Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Port ObservatoryWorldCanvas + build ObservatoryTab + swap /observatory route</name>
  <files>
    apps/workbench/src/features/observatory/components/ObservatoryWorldCanvas.tsx
    apps/workbench/src/features/observatory/components/ObservatoryTab.tsx
    apps/workbench/src/components/desktop/workbench-routes.tsx
  </files>

  <read_first>
    - /Users/connor/Medica/backbay/standalone/clawdstrike-worktrees/huntronomer-workspace-orch/apps/desktop/src/features/hunt-observatory/world/ObservatoryWorldCanvas.tsx (primary source — port with huntronomer-specific removal)
    - apps/workbench/src/features/spirit/components/spirit-companion-canvas.tsx (established workbench Canvas pattern: frameloop="demand", dpr=[1,1.8], absolute inset-0)
    - apps/workbench/src/features/spirit/stores/spirit-store.ts (SpiritKind type: sentinel|oracle|witness|specter)
    - apps/workbench/src/features/observatory/world/types.ts (just created — HuntObservatorySceneState, HuntStationId)
    - apps/workbench/src/features/observatory/world/stations.ts (just created — HUNT_STATION_PLACEMENTS, HUNT_STATION_LABELS)
    - apps/workbench/src/features/observatory/stores/observatory-store.ts (useObservatoryStore.use.stations())
    - apps/workbench/src/components/desktop/workbench-routes.tsx (WebGLSpikeCanvas to replace)
    - .planning/phases/03-full-immersive-panes-observatory-forensics/03-RESEARCH.md (Pattern 1: ObservatoryTab as Store Bridge, Pattern 5: Canvas Sizing)
  </read_first>

  <action>
    **ObservatoryWorldCanvas.tsx** — Port from huntronomer with targeted removals:
    KEEP: station mesh rendering (spheres with emissive material colored by station type), drei Text labels above each station, camera rig (port WorldCameraRig with bezier flight + smoothstep easing from huntronomer), ambient + directional lighting, OrbitControls for atlas mode, probe ring visuals (ring mesh expanding from station position during probe animation), flow mode atmosphere/terrain meshes.
    REMOVE: mission system imports (missionLoop.ts), OpenClaw context (useOpenClaw, useLiveMission), NexusAppRail, session routing (onSelectStation that navigates via router), NavRail components.
    STRIP: Any import from huntronomer's selectors/ or from contexts that don't exist in the workbench.
    ADAPT: Props interface — accept `{ mode: "atlas" | "flow", sceneState: HuntObservatorySceneState | null, activeStationId: HuntStationId | null, spirit: { kind: string; accentColor: string } | null, frameloop?: "demand" | "always", onSelectStation?: (id: HuntStationId) => void }`.
    HERO PROPS: Force all hero props to use fallback procedural geometry — set availability to "slot" for all entries or strip the useGLTF branch entirely. DO NOT add GLB asset URLs. Phase 3 uses fallback geometry only (see pitfall 3 in RESEARCH.md).
    PHYSICS: Conditional render — `<Physics>` wraps only when `mode === "flow"` AND character controller is enabled (passed as prop `characterControllerEnabled: boolean`). In atlas mode: no Physics wrapper. This avoids the always-on overhead (pitfall 1 in RESEARCH.md).
    Canvas config: `frameloop` is a prop (defaults "demand"), `dpr={[1, 1.8]}`, `gl={{ antialias: true, alpha: false, powerPreference: "high-performance" }}`.
    HuntStationId mapping for workbench stations: Workbench uses "signal"|"targets"|"run"|"receipts"|"case-notes"|"watch" as the synthetic fixed set (from RESEARCH.md code example). Accept the pre-built HuntObservatorySceneState from ObservatoryTab — no mapping needed inside the canvas.

    **ObservatoryTab.tsx** — Build the store bridge wrapper following Pattern 1 in RESEARCH.md exactly.
    NOTE ON PATTERN 1: ObservatoryTab acting as the immediate parent component that constructs sceneState and passes it as props to ObservatoryWorldCanvas IS Pattern 1 (correct). The anti-pattern in RESEARCH.md's anti-patterns list ("Props-drilling workbench store state") refers to passing raw workbench store slices (ObservatoryStation[]) through multiple intermediate layers down to the canvas. Here ObservatoryTab reads the stores and builds the HuntObservatorySceneState in one place — that is the intended pattern.
    - Local state: `mode` ("atlas" | "flow"), `characterControllerEnabled` (false)
    - Reads: `useObservatoryStore.use.stations()`, `useSpiritStore.use.kind()`, `useSpiritStore.use.accentColor()`
    - Builds `HuntObservatorySceneState` using the WORKBENCH_STATION_IDS fixed set (["signal","targets","run","receipts","case-notes","watch"]) from world/stations.ts HUNT_STATION_PLACEMENTS. Maps workbench station artifactCount to corresponding HuntStationId by index position (station[0] → signal, etc.). If workbench has no stations, map all artifactCount to 0.
    - SpiritKind mapping to ObservatorySpiritVisual.kind: sentinel→"tracker", oracle→"lantern", witness→"ledger", specter→"forge"
    - Outer wrapper: `<div className="relative flex-1 overflow-hidden">` → `<div className="absolute inset-0">` → `<ObservatoryWorldCanvas ... />`
    - Expose setMode and setCharacterControllerEnabled via component state — Plan 03-02 will need setMode for flow mode toggle button.
    - Export: `ObservatoryTab` named export.

    **workbench-routes.tsx** — Replace the WebGLSpikeCanvas lazy import and route:
    - Remove: `const WebGLSpikeCanvas = lazy(() => import(...).then(...))` block
    - Add: `const ObservatoryTab = lazy(() => import("@/features/observatory/components/ObservatoryTab").then((m) => ({ default: m.ObservatoryTab })))`
    - Route change: `{ path: "observatory", element: <Suspense fallback={<div className="flex-1" />}><ObservatoryTab /></Suspense> }`
    - Leave spirit-chamber and nexus routes untouched.

    Run `npm test --workspace=apps/workbench -- observatory-tab --reporter=dot` — observatory-tab.test.tsx tests should pass (GREEN). Commit: "feat(03-01): port ObservatoryWorldCanvas, build ObservatoryTab, swap /observatory route".
  </action>

  <verify>
    <automated>npm test --workspace=apps/workbench -- observatory-tab --reporter=dot</automated>
  </verify>

  <acceptance_criteria>
    - grep "ObservatoryWorldCanvas" apps/workbench/src/features/observatory/components/ObservatoryWorldCanvas.tsx → component defined
    - grep "frameloop" apps/workbench/src/features/observatory/components/ObservatoryWorldCanvas.tsx → frameloop is a prop (not hardcoded)
    - grep "Physics" apps/workbench/src/features/observatory/components/ObservatoryWorldCanvas.tsx → Physics only rendered conditionally (grep for "mode === \"flow\"" nearby)
    - grep "useGLTF" apps/workbench/src/features/observatory/components/ObservatoryWorldCanvas.tsx → should NOT be present (fallback geometry only)
    - grep "ObservatoryTab" apps/workbench/src/features/observatory/components/ObservatoryTab.tsx → component exported
    - grep "absolute inset-0" apps/workbench/src/features/observatory/components/ObservatoryTab.tsx → canvas sizing pattern present
    - grep "WORKBENCH_STATION_IDS\|signal.*targets.*run\|HuntStationId" apps/workbench/src/features/observatory/components/ObservatoryTab.tsx → fixed station ID set used
    - grep "WebGLSpikeCanvas" apps/workbench/src/components/desktop/workbench-routes.tsx → should NOT be present
    - grep "ObservatoryTab" apps/workbench/src/components/desktop/workbench-routes.tsx → present in route
    - npm test observatory-tab → PASS
    - npm test --workspace=apps/workbench → full suite still green (no regressions)
  </acceptance_criteria>

  <done>ObservatoryWorldCanvas renders 6 station spheres in atlas mode. ObservatoryTab bridges workbench stores to canvas. /observatory route shows ObservatoryTab not WebGLSpikeCanvas. observatory-tab tests pass GREEN.</done>
</task>

</tasks>

<verification>
Full suite: `npm test --workspace=apps/workbench`
Route verification: navigate to /observatory in dev — 3D station sphere grid fills full pane.
No regressions: all Phase 1 and Phase 2 tests still pass.
</verification>

<success_criteria>
- ObservatoryTab renders at /observatory (WebGLSpikeCanvas gone)
- 6 station spheres render in atlas mode via HUNT_STATION_PLACEMENTS
- deriveObservatoryWorld, probeRuntime, types, stations all exported from world/ directory
- All 3 test files exist; observatory-tab and probe-runtime tests pass GREEN
- Full workbench test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/03-full-immersive-panes-observatory-forensics/03-01-SUMMARY.md`
</output>
