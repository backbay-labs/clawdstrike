---
phase: 03-full-immersive-panes-observatory-forensics
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - apps/workbench/package.json
  - apps/workbench/src/features/observatory/character/types.ts
  - apps/workbench/src/features/observatory/character/input/useObservatoryPlayerInput.ts
  - apps/workbench/src/features/observatory/character/controller/runtime.ts
  - apps/workbench/src/features/observatory/character/controller/useObservatoryPlayerRuntime.ts
  - apps/workbench/src/features/observatory/character/physics/colliders.ts
  - apps/workbench/src/features/observatory/character/physics/spawn.ts
  - apps/workbench/src/features/observatory/components/FlowModeController.tsx
  - apps/workbench/src/features/observatory/components/ObservatoryWorldCanvas.tsx
  - apps/workbench/src/features/observatory/components/ObservatoryTab.tsx
autonomous: true
requirements:
  - OBS-06
user_setup: []

must_haves:
  truths:
    - "WASD character controller only activates in flow mode after a double-click (Easter-egg)"
    - "Rapier/ecctrl are lazy-loaded — main bundle does NOT import @react-three/rapier at module level"
    - "A toast notification 'WASD controls activated' appears when character controller Easter-egg is triggered"
    - "Physics wrapper is absent from the scene when character controller is not active (no Rapier overhead in atlas or plain flow mode)"
    - "Player can move with WASD keys when character controller is active — input hook is disabled unless flow mode + Easter-egg active"
  artifacts:
    - path: "apps/workbench/src/features/observatory/character/types.ts"
      provides: "ObservatoryPlayerState, ObservatoryPlayerConfig, DEFAULT_OBSERVATORY_PLAYER_CONFIG, ObservatoryPlayerBindings — ported from huntronomer character/types.ts"
      exports: ["ObservatoryPlayerState", "DEFAULT_OBSERVATORY_PLAYER_CONFIG", "DEFAULT_OBSERVATORY_PLAYER_SPAWN"]
    - path: "apps/workbench/src/features/observatory/components/FlowModeController.tsx"
      provides: "Lazy-loaded Rapier Physics + player capsule collider + WASD input hook — renders only when characterControllerEnabled=true"
      exports: ["FlowModeController"]
    - path: "apps/workbench/src/features/observatory/components/ObservatoryTab.tsx"
      provides: "double-click handler in flow mode activates character controller + shows toast"
      contains: "characterControllerEnabled, onDoubleClick"
  key_links:
    - from: "apps/workbench/src/features/observatory/components/ObservatoryTab.tsx"
      to: "apps/workbench/src/features/observatory/components/FlowModeController.tsx"
      via: "React.lazy + Suspense — only rendered when mode==='flow' && characterControllerEnabled"
      pattern: "React\\.lazy.*FlowModeController"
    - from: "apps/workbench/src/features/observatory/components/FlowModeController.tsx"
      to: "@react-three/rapier"
      via: "Physics import — lazy module boundary prevents main bundle inclusion"
      pattern: "from.*@react-three/rapier"
    - from: "apps/workbench/src/features/observatory/components/ObservatoryWorldCanvas.tsx"
      to: "apps/workbench/src/features/observatory/components/FlowModeController.tsx"
      via: "conditional render when mode==='flow' && characterControllerEnabled"
      pattern: "FlowModeController"
---

<objective>
Port the WASD character controller Easter-egg from huntronomer. Rapier + ecctrl are added to package.json and lazy-loaded only when the user activates flow mode and double-clicks. Physics only wraps the scene when the controller is active.

Purpose: Delivers OBS-06 — character controller Easter-egg in observatory flow mode.
Output: character/ subsystem (types, input, controller, physics — ported verbatim), FlowModeController (lazy-loaded Rapier wrapper), double-click handler in ObservatoryTab, toast notification.
</objective>

<execution_context>
@/Users/connor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-full-immersive-panes-observatory-forensics/03-CONTEXT.md
@.planning/phases/03-full-immersive-panes-observatory-forensics/03-RESEARCH.md
@.planning/phases/03-full-immersive-panes-observatory-forensics/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Rapier + ecctrl + port character subsystem types and pure functions</name>
  <files>
    apps/workbench/package.json
    apps/workbench/src/features/observatory/character/types.ts
    apps/workbench/src/features/observatory/character/controller/runtime.ts
    apps/workbench/src/features/observatory/character/physics/colliders.ts
    apps/workbench/src/features/observatory/character/physics/spawn.ts
  </files>

  <read_first>
    - /Users/connor/Medica/backbay/standalone/clawdstrike-worktrees/huntronomer-workspace-orch/apps/desktop/src/features/hunt-observatory/character/types.ts (port verbatim — already read above)
    - /Users/connor/Medica/backbay/standalone/clawdstrike-worktrees/huntronomer-workspace-orch/apps/desktop/src/features/hunt-observatory/character/controller/runtime.ts (port verbatim)
    - /Users/connor/Medica/backbay/standalone/clawdstrike-worktrees/huntronomer-workspace-orch/apps/desktop/src/features/hunt-observatory/character/physics/colliders.ts (port verbatim)
    - /Users/connor/Medica/backbay/standalone/clawdstrike-worktrees/huntronomer-workspace-orch/apps/desktop/src/features/hunt-observatory/character/physics/spawn.ts (port verbatim)
    - apps/workbench/package.json (check current devDependencies before modifying)
  </read_first>

  <action>
    **Install packages** (character controller task only — per RESEARCH.md):
    ```bash
    npm install @react-three/rapier@^2.2.0 ecctrl@^1.0.97 --workspace=apps/workbench
    ```
    Verify both appear in apps/workbench/package.json dependencies (not devDependencies).

    **Port character types and pure functions** — create the character/ directory structure under apps/workbench/src/features/observatory/:
    ```
    character/
    ├── types.ts         (from huntronomer character/types.ts — verbatim)
    ├── controller/
    │   └── runtime.ts   (from huntronomer character/controller/runtime.ts — verbatim)
    ├── physics/
    │   ├── colliders.ts (from huntronomer character/physics/colliders.ts — verbatim)
    │   └── spawn.ts     (from huntronomer character/physics/spawn.ts — verbatim)
    ```

    **character/types.ts**: Port huntronomer character/types.ts verbatim. Contains ObservatoryPlayerState, ObservatoryPlayerConfig, ObservatoryPlayerBindings, ObservatoryPlayerIntent, DEFAULT_OBSERVATORY_PLAYER_BINDINGS (WASD+Space+Shift), DEFAULT_OBSERVATORY_PLAYER_CONFIG (walkSpeed 4.4, sprintMultiplier 1.55, gravity 18), DEFAULT_OBSERVATORY_PLAYER_SPAWN (position [0, 1.18, 6.6]), createInitialObservatoryPlayerState, buildStationSpawnPoint. Remap imports to local world/types.ts for HuntStationId + HuntStationPlacement.

    **controller/runtime.ts**: Port huntronomer controller/runtime.ts verbatim. Pure functions: stepObservatoryPlayerState, createObservatoryPlayerStateFromSpawn. Remap imports to local character/types.ts.

    **physics/colliders.ts**: Port huntronomer physics/colliders.ts verbatim. createObservatoryBoundaryColliders, createObservatoryPlayerCapsuleCollider, createStationPlateSpecs. These return ObservatoryColliderSpec[] arrays — pure data, no Rapier imports at this level.

    **physics/spawn.ts**: Port huntronomer physics/spawn.ts verbatim. resolveObservatoryPlayerSpawnPoint — pure calculation.

    DO NOT create the React hooks yet (useObservatoryPlayerInput, useObservatoryPlayerRuntime) — those go in Task 2 with the lazy-loaded FlowModeController.

    Run `npm test --workspace=apps/workbench -- --reporter=dot` — full suite should still pass. Commit: "feat(03-03): install rapier+ecctrl, port character types and pure functions".
  </action>

  <verify>
    <automated>npm test --workspace=apps/workbench -- --reporter=dot</automated>
  </verify>

  <acceptance_criteria>
    - grep "@react-three/rapier" apps/workbench/package.json → "^2.2.0" present
    - grep "ecctrl" apps/workbench/package.json → "^1.0.97" present
    - grep "DEFAULT_OBSERVATORY_PLAYER_CONFIG" apps/workbench/src/features/observatory/character/types.ts → exported
    - grep "walkSpeed.*4.4" apps/workbench/src/features/observatory/character/types.ts → correct value
    - grep "stepObservatoryPlayerState" apps/workbench/src/features/observatory/character/controller/runtime.ts → pure function exported
    - grep "createObservatoryBoundaryColliders" apps/workbench/src/features/observatory/character/physics/colliders.ts → exported
    - grep "from.*@react-three/rapier" apps/workbench/src/features/observatory/character/types.ts → should NOT appear (types.ts is pure)
    - npm test --workspace=apps/workbench → full suite PASS
  </acceptance_criteria>

  <done>Rapier + ecctrl installed. Character subsystem pure-function layer ported. No Rapier imports at module level in types.ts or runtime.ts — lazy boundary maintained.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FlowModeController lazy-loaded component + input hook</name>
  <files>
    apps/workbench/src/features/observatory/character/input/useObservatoryPlayerInput.ts
    apps/workbench/src/features/observatory/character/controller/useObservatoryPlayerRuntime.ts
    apps/workbench/src/features/observatory/components/FlowModeController.tsx
  </files>

  <read_first>
    - /Users/connor/Medica/backbay/standalone/clawdstrike-worktrees/huntronomer-workspace-orch/apps/desktop/src/features/hunt-observatory/character/input/useObservatoryPlayerInput.ts (port verbatim — keyboard → ObservatoryPlayerIntent hook)
    - /Users/connor/Medica/backbay/standalone/clawdstrike-worktrees/huntronomer-workspace-orch/apps/desktop/src/features/hunt-observatory/character/controller/useObservatoryPlayerRuntime.ts (port verbatim)
    - /Users/connor/Medica/backbay/standalone/clawdstrike-worktrees/huntronomer-workspace-orch/apps/desktop/src/features/hunt-observatory/character/controller/ObservatoryPlayerController.tsx (primary source for FlowModeController — Physics + RigidBody + player mesh)
    - apps/workbench/src/features/observatory/__tests__/flow-mode-controller.test.tsx (Wave 0 test scaffold — make these pass)
    - .planning/phases/03-full-immersive-panes-observatory-forensics/03-RESEARCH.md (Pattern 3: lazy + Suspense; pitfall 5: keyboard input scope)
  </read_first>

  <behavior>
    FlowModeController:
    - Renders null when characterControllerEnabled = false (prop)
    - When enabled=true: renders Physics > RigidBody with CapsuleCollider > ObservatoryPlayerAvatar (simple capsule mesh — skip GLB avatar, use MeshStandardMaterial capsule)
    - useObservatoryPlayerInput enabled only when pane is active AND flow mode AND characterControllerEnabled
    - useObservatoryPlayerRuntime drives RigidBody position each frame via useFrame
    flow-mode-controller.test.tsx:
    - FlowModeController(enabled=false) → renders null
    - FlowModeController(enabled=true) → renders a Physics wrapper (mock Rapier)
  </behavior>

  <action>
    **character/input/useObservatoryPlayerInput.ts**: Port huntronomer's useObservatoryPlayerInput.ts verbatim. Handles WASD → ObservatoryPlayerIntent. Key implementation detail: the hook accepts an `enabled` option — when `enabled: false`, the event listeners are not attached (pitfall 5: keyboard scope). Remap imports to local character/types.ts.

    **character/controller/useObservatoryPlayerRuntime.ts**: Port huntronomer's useObservatoryPlayerRuntime.ts verbatim. Maintains ObservatoryPlayerState using the pure stepObservatoryPlayerState function and reads Rapier RigidBody snapshot. Remap imports to local character/ paths.

    **FlowModeController.tsx**: Build the lazy-load boundary component at apps/workbench/src/features/observatory/components/FlowModeController.tsx. THIS FILE imports from @react-three/rapier — it is the lazy module boundary. It renders null when enabled=false, which is the common case.

    ```typescript
    // FlowModeController.tsx — imports Rapier here (lazy boundary)
    import { Physics, RigidBody, CapsuleCollider } from "@react-three/rapier";
    import { useRef } from "react";
    import { useObservatoryPlayerInput } from "../character/input/useObservatoryPlayerInput";
    import { useObservatoryPlayerRuntime } from "../character/controller/useObservatoryPlayerRuntime";
    import { DEFAULT_OBSERVATORY_PLAYER_SPAWN } from "../character/types";
    import { createObservatoryBoundaryColliders, createStationPlateSpecs } from "../character/physics/colliders";

    interface FlowModeControllerProps {
      enabled: boolean;
      paneIsActive: boolean;
    }

    export function FlowModeController({ enabled, paneIsActive }: FlowModeControllerProps) {
      if (!enabled) return null;
      // ... render Physics wrapper with RigidBody + CapsuleCollider + capsule mesh for player
      // Use useObservatoryPlayerInput({ enabled: paneIsActive && enabled })
      // Use useObservatoryPlayerRuntime to drive RigidBody position each frame
      // Player avatar: simple <mesh><capsuleGeometry args={[0.3, 1.0, 8, 16]} /><meshStandardMaterial color="#3dbf84" /></mesh>
      // Boundary colliders: map createObservatoryBoundaryColliders() → <CuboidCollider> elements
    }
    ```

    Pitfall 5 (RESEARCH.md): Pass `enabled: paneIsActive && enabled` to useObservatoryPlayerInput. This prevents WASD from consuming editor keyboard events when the user isn't focused on the observatory pane.

    Pitfall 1 (RESEARCH.md): The Physics wrapper is INSIDE FlowModeController which renders null when disabled. This ensures no Rapier tick overhead when the Easter-egg is off.

    Run `npm test --workspace=apps/workbench -- flow-mode-controller --reporter=dot` — flow-mode-controller tests should now pass (GREEN). Commit: "feat(03-03): FlowModeController lazy-loaded + player input hook".
  </action>

  <verify>
    <automated>npm test --workspace=apps/workbench -- flow-mode-controller --reporter=dot</automated>
  </verify>

  <acceptance_criteria>
    - grep "from.*@react-three/rapier" apps/workbench/src/features/observatory/components/FlowModeController.tsx → Rapier import present (this is the lazy boundary)
    - grep "from.*@react-three/rapier" apps/workbench/src/features/observatory/character/types.ts → should NOT be present
    - grep "from.*@react-three/rapier" apps/workbench/src/features/observatory/character/controller/runtime.ts → should NOT be present
    - grep "if.*!enabled.*return null" apps/workbench/src/features/observatory/components/FlowModeController.tsx → null guard present
    - grep "enabled.*paneIsActive" apps/workbench/src/features/observatory/components/FlowModeController.tsx → input hook enabled only when both conditions true
    - npm test flow-mode-controller → PASS
  </acceptance_criteria>

  <done>FlowModeController renders null by default. When enabled, Physics + RigidBody + CapsuleCollider active. Rapier imports isolated to this file. Input hook only fires when pane is active. Tests GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Wire lazy FlowModeController into ObservatoryWorldCanvas + double-click Easter-egg in ObservatoryTab</name>
  <files>
    apps/workbench/src/features/observatory/components/ObservatoryWorldCanvas.tsx
    apps/workbench/src/features/observatory/components/ObservatoryTab.tsx
  </files>

  <read_first>
    - apps/workbench/src/features/observatory/components/ObservatoryWorldCanvas.tsx (from 03-02 — extend with lazy FlowModeController)
    - apps/workbench/src/features/observatory/components/ObservatoryTab.tsx (from 03-02 — add double-click handler)
    - .planning/phases/03-full-immersive-panes-observatory-forensics/03-CONTEXT.md (Easter-egg activation: double-click in flow mode shows toast "WASD controls activated")
    - .planning/phases/03-full-immersive-panes-observatory-forensics/03-RESEARCH.md (Pattern 3: lazy FlowModeController in Suspense)
  </read_first>

  <action>
    **ObservatoryWorldCanvas.tsx — lazy FlowModeController render:**
    At the TOP of the file (outside the component), add the lazy import:
    ```typescript
    const FlowModeController = React.lazy(() =>
      import("./FlowModeController").then((m) => ({ default: m.FlowModeController }))
    );
    ```
    Accept new prop `characterControllerEnabled: boolean` (default false) and `paneIsActive: boolean` (default false).
    Inside the Canvas JSX, conditionally render after the scene geometry:
    ```tsx
    {mode === "flow" && characterControllerEnabled && (
      <Suspense fallback={null}>
        <FlowModeController enabled={true} paneIsActive={paneIsActive} />
      </Suspense>
    )}
    ```
    Note: No `<Physics>` at canvas level — Physics lives inside FlowModeController itself (guaranteed no Rapier overhead when disabled per pitfall 1).

    **ObservatoryTab.tsx — double-click Easter-egg:**
    Add `characterControllerEnabled` state (useState false).
    Add toast notification. Use the Sonner toast library (already in workbench — check existing usage with `grep -r "sonner\|toast(" apps/workbench/src/` to confirm import path). If sonner not available, use a simple temporary DOM notification.
    Add onDoubleClick handler on the outer container div (the `relative flex-1 overflow-hidden` wrapper):
    ```typescript
    const handleDoubleClick = useCallback(() => {
      if (mode !== "flow") return;
      setCharacterControllerEnabled((prev) => {
        const next = !prev;
        toast(next ? "WASD controls activated" : "WASD controls deactivated");
        return next;
      });
    }, [mode]);
    ```
    Pass characterControllerEnabled and paneIsActive to ObservatoryWorldCanvas.

    paneIsActive: use a simple approach — read from usePaneStore to detect if the /observatory route is currently the active pane. Check usePaneStore.use.activePaneId() or equivalent selector that exists in the workbench pane-store (read pane-store.ts to find the correct selector before implementing).

    Run full suite: `npm test --workspace=apps/workbench -- --reporter=dot`. Commit: "feat(03-03): lazy FlowModeController wired + double-click Easter-egg with toast".
  </action>

  <verify>
    <automated>npm test --workspace=apps/workbench -- --reporter=dot</automated>
  </verify>

  <acceptance_criteria>
    - grep "React\.lazy.*FlowModeController" apps/workbench/src/features/observatory/components/ObservatoryWorldCanvas.tsx → lazy import at module top
    - grep "mode.*flow.*characterControllerEnabled\|characterControllerEnabled.*mode.*flow" apps/workbench/src/features/observatory/components/ObservatoryWorldCanvas.tsx → conditional render gate
    - grep "Suspense" apps/workbench/src/features/observatory/components/ObservatoryWorldCanvas.tsx → Suspense wraps FlowModeController
    - grep "characterControllerEnabled" apps/workbench/src/features/observatory/components/ObservatoryTab.tsx → state present
    - grep "handleDoubleClick\|onDoubleClick" apps/workbench/src/features/observatory/components/ObservatoryTab.tsx → double-click handler wired
    - grep "toast\|WASD controls activated" apps/workbench/src/features/observatory/components/ObservatoryTab.tsx → toast notification present
    - grep "mode.*flow.*return" apps/workbench/src/features/observatory/components/ObservatoryTab.tsx → guard: only activates in flow mode
    - npm test --workspace=apps/workbench → full suite PASS
  </acceptance_criteria>

  <done>Double-click in flow mode toggles character controller Easter-egg. Toast "WASD controls activated" fires. FlowModeController lazy-loads Rapier only when needed. Physics wraps only when active. Full suite green.</done>
</task>

</tasks>

<verification>
Full suite: `npm test --workspace=apps/workbench`
Bundle check: `grep -r "react-three/rapier" apps/workbench/src/ | grep -v FlowModeController` → should return nothing (Rapier only imported in FlowModeController)
Manual: Switch to flow mode → double-click → toast fires → WASD moves player.
</verification>

<success_criteria>
- @react-three/rapier and ecctrl installed in apps/workbench/package.json
- Character subsystem ported under features/observatory/character/
- FlowModeController lazy-loaded — Rapier not in main bundle
- Double-click in flow mode activates Easter-egg + toast
- Physics only active when controller enabled
- Input hook only steals keyboard when pane is active AND flow mode AND Easter-egg on
- Full workbench test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/03-full-immersive-panes-observatory-forensics/03-03-SUMMARY.md`
</output>
