---
phase: 22-space-environment-art
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/workbench/src/features/observatory/components/world-canvas/ObservatoryNebulaClouds.tsx
  - apps/workbench/src/features/observatory/components/world-canvas/ObservatoryWorldScene.tsx
  - apps/workbench/src/features/observatory/__tests__/observatory-nebula-clouds.test.tsx
autonomous: true
requirements: [SPC-03]

must_haves:
  truths:
    - "Billboard nebula cloud patches are visible near stations during flight approach"
    - "Each station cluster has 2-3 cloud patches colored to match station colorHex"
    - "Cloud patches glow through bloom via toneMapped=false point lights"
    - "Patches use AdditiveBlending for ethereal translucent appearance"
  artifacts:
    - path: "apps/workbench/src/features/observatory/components/world-canvas/ObservatoryNebulaClouds.tsx"
      provides: "Billboard nebula cloud patches positioned near each station"
      exports: ["ObservatoryNebulaClouds"]
  key_links:
    - from: "ObservatoryNebulaClouds.tsx"
      to: "observatory-world-template.ts"
      via: "OBSERVATORY_STATION_POSITIONS for patch placement"
      pattern: "OBSERVATORY_STATION_POSITIONS"
    - from: "ObservatoryWorldScene.tsx"
      to: "ObservatoryNebulaClouds.tsx"
      via: "JSX child render"
      pattern: "<ObservatoryNebulaClouds"
---

<objective>
Billboard nebula cloud patches near each station with colored glow through bloom

Purpose: Add ethereal nebula clouds near stations so they serve as visible "auras" during approach — color-coded per station, glowing through the bloom pipeline. The clouds break up the uniform void and provide visual wayfinding cues.

Output: ObservatoryNebulaClouds component wired into ObservatoryWorldScene
</objective>

<execution_context>
@/Users/connor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-space-environment-art/22-CONTEXT.md

<interfaces>
<!-- Station positions and colors for patch placement -->

From observatory-world-template.ts:
```typescript
export const WORLD_RADIUS = 300;
export const OBSERVATORY_STATION_POSITIONS: Record<HuntStationId, ObservatoryVec3>;
// positions like: signal: [~180, 25, ~220], targets: [~-120, 45, ~280], etc.

const STATION_COLORS: Record<HuntStationId, string> = {
  signal: "#7cc8ff",
  targets: "#9df2dd",
  run: "#f4d982",
  receipts: "#7ee6f2",
  "case-notes": "#f0b87b",
  watch: "#d3b56e",
};
```

From ObservatoryPostFX.tsx (bloom pipeline):
```typescript
// Bloom with luminanceThreshold, mipmapBlur
// Materials with toneMapped={false} and emissiveIntensity > 1 glow through bloom
```

From ObservatoryWorldScene.tsx:
```typescript
// Environment elements rendered as children inside the scene fragment
// Pattern: import component, render as <ComponentName /> sibling to lights/fog
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ObservatoryNebulaClouds component with billboard patches + colored point lights</name>
  <files>
    apps/workbench/src/features/observatory/components/world-canvas/ObservatoryNebulaClouds.tsx,
    apps/workbench/src/features/observatory/__tests__/observatory-nebula-clouds.test.tsx
  </files>
  <read_first>
    apps/workbench/src/features/observatory/world/observatory-world-template.ts,
    apps/workbench/src/features/observatory/components/world-canvas/ObservatoryWorldScene.tsx
  </read_first>
  <behavior>
    - Test 1: ObservatoryNebulaClouds exports a named React component
    - Test 2: Component renders without crashing in a test R3F canvas
  </behavior>
  <action>
    1. Create `apps/workbench/src/features/observatory/components/world-canvas/ObservatoryNebulaClouds.tsx`:

       - Import OBSERVATORY_STATION_POSITIONS from observatory-world-template.ts.
       - Import HUNT_PRIMARY_STATION_ORDER from stations.ts (the 6 station IDs).
       - Define STATION_COLORS inline (same map as in observatory-world-template.ts: signal="#7cc8ff", targets="#9df2dd", run="#f4d982", receipts="#7ee6f2", case-notes="#f0b87b", watch="#d3b56e").
       - For each station, generate 2-3 cloud patches using a deterministic seed (use mulberry32 or simple hash of station ID + patch index). Each patch:
         - PlaneGeometry(8, 8) with MeshBasicMaterial
         - Billboard behavior: use drei `<Billboard>` wrapper OR set `onBeforeRender` to always face camera. drei Billboard is simpler.
         - Material: `transparent: true`, `opacity: 0.4-0.6` (vary per patch), `color: station colorHex`, `blending: THREE.AdditiveBlending`, `depthWrite: false`, `toneMapped: false`
         - For the texture: create a radial gradient procedurally using Canvas2D at init time (useMemo). Draw a 128x128 canvas with a radial gradient from white center to transparent edge. Create THREE.CanvasTexture from it. This avoids needing an external PNG asset.
         - Position: station world position + random offset within +/-20 units on X/Z, +/-10 units on Y
         - Random Z rotation for variety: `rotation-z={seed * Math.PI * 2}`
       - For each station, add ONE PointLight at the station position (or cluster center):
         - `color={stationColor}`, `intensity={2.0}`, `distance={60}`, `decay={2}`
         - `toneMapped={false}` — not a prop on PointLight but the material glow comes from AdditiveBlending + bloom pipeline interaction

       - Use useMemo to pre-compute all patch positions/rotations/opacities at init time (no per-frame allocation).
       - Export as `ObservatoryNebulaClouds`.

    2. Create test file verifying smoke render.

    Note: Frustum-culling is handled automatically by Three.js at this scale — no custom LOD needed per user decision.
  </action>
  <verify>
    <automated>cd apps/workbench && npx vitest run src/features/observatory/__tests__/observatory-nebula-clouds.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "ObservatoryNebulaClouds" apps/workbench/src/features/observatory/components/world-canvas/ObservatoryNebulaClouds.tsx
    - grep -q "Billboard" apps/workbench/src/features/observatory/components/world-canvas/ObservatoryNebulaClouds.tsx
    - grep -q "AdditiveBlending" apps/workbench/src/features/observatory/components/world-canvas/ObservatoryNebulaClouds.tsx
    - grep -q "pointLight\|PointLight\|pointlight" apps/workbench/src/features/observatory/components/world-canvas/ObservatoryNebulaClouds.tsx
    - grep -q "depthWrite.*false\|depthWrite:.*false" apps/workbench/src/features/observatory/components/world-canvas/ObservatoryNebulaClouds.tsx
  </acceptance_criteria>
  <done>
    ObservatoryNebulaClouds renders 2-3 billboard cloud patches per station with AdditiveBlending, procedural radial gradient texture, station-colored point lights; all positioned near station world coordinates with deterministic random offsets
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire ObservatoryNebulaClouds into ObservatoryWorldScene</name>
  <files>
    apps/workbench/src/features/observatory/components/world-canvas/ObservatoryWorldScene.tsx
  </files>
  <read_first>
    apps/workbench/src/features/observatory/components/world-canvas/ObservatoryWorldScene.tsx,
    apps/workbench/src/features/observatory/components/world-canvas/ObservatoryNebulaClouds.tsx
  </read_first>
  <action>
    1. In ObservatoryWorldScene.tsx:
       - Add import: `import { ObservatoryNebulaClouds } from "./ObservatoryNebulaClouds";`
       - Add `<ObservatoryNebulaClouds />` as a child in the scene fragment, after the lighting block and before the camera controls. Position it near the other environment elements (stars/fog area).

    2. Run the full observatory test suite to confirm no regressions.

    Note: This plan may execute in parallel with Plan 01 (starfield). Both modify ObservatoryWorldScene.tsx, but add independent import lines and JSX children — no conflict if Plan 01 lands first or this lands first. If both run in the same wave, the executor should read the latest file state before editing.
  </action>
  <verify>
    <automated>cd apps/workbench && npx vitest run src/features/observatory/__tests__/ --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "ObservatoryNebulaClouds" apps/workbench/src/features/observatory/components/world-canvas/ObservatoryWorldScene.tsx
    - grep -q "import.*ObservatoryNebulaClouds" apps/workbench/src/features/observatory/components/world-canvas/ObservatoryWorldScene.tsx
  </acceptance_criteria>
  <done>
    ObservatoryWorldScene renders nebula cloud patches near all 6 stations; all existing observatory tests pass
  </done>
</task>

</tasks>

<verification>
1. `cd apps/workbench && npx vitest run src/features/observatory/__tests__/ --reporter=verbose` — all tests pass
2. `npx tsc --noEmit` in apps/workbench — no TypeScript errors
3. ObservatoryNebulaClouds renders billboard patches with station-specific colors
4. Each station has point light for bloom glow
</verification>

<success_criteria>
- 2-3 billboard cloud patches per station with AdditiveBlending and procedural radial gradient texture
- PointLight per station cluster colored by station colorHex
- All patches use depthWrite:false, toneMapped:false, transparent:true
- Wired into ObservatoryWorldScene as a child element
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/22-space-environment-art/22-02-SUMMARY.md`
</output>
