---
phase: 22-space-environment-art
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/workbench/src/features/observatory/components/world-canvas/ObservatoryStarfield.tsx
  - apps/workbench/src/features/observatory/components/world-canvas/ObservatoryWorldScene.tsx
  - apps/workbench/src/features/observatory/shaders/starNest.glsl
  - apps/workbench/src/features/observatory/__tests__/observatory-starfield.test.tsx
autonomous: true
requirements: [SPC-02, SPC-04]

must_haves:
  truths:
    - "Three distinct star layers are visible during flight — far procedural shader, mid-field instanced stars, near sparkle dust"
    - "Parallax between layers is perceptible when the ship moves"
    - "Depth fog fades distant stations into void rather than hard-cutting"
    - "Star background sphere renders behind fog (depthWrite: false)"
  artifacts:
    - path: "apps/workbench/src/features/observatory/shaders/starNest.glsl"
      provides: "Star Nest fragment shader (Shadertoy XlfGRj port)"
      contains: "volsteps"
    - path: "apps/workbench/src/features/observatory/components/world-canvas/ObservatoryStarfield.tsx"
      provides: "3-layer starfield: ShaderMaterial sphere + InstancedMesh mid-field + Sparkles near-dust"
      exports: ["ObservatoryStarfield"]
    - path: "apps/workbench/src/features/observatory/components/world-canvas/ObservatoryWorldScene.tsx"
      provides: "Updated scene with starfield + FogExp2 replacing Stars + linear fog"
      contains: "ObservatoryStarfield"
  key_links:
    - from: "ObservatoryStarfield.tsx"
      to: "starNest.glsl"
      via: "raw GLSL import for ShaderMaterial"
      pattern: "import.*starNest"
    - from: "ObservatoryWorldScene.tsx"
      to: "ObservatoryStarfield.tsx"
      via: "JSX child render"
      pattern: "<ObservatoryStarfield"
    - from: "ObservatoryWorldScene.tsx"
      to: "THREE.FogExp2"
      via: "fog attach replacement"
      pattern: "FogExp2"
---

<objective>
3-layer deep space starfield and exponential depth fog — the void between stations feels like deep space

Purpose: Replace the drei Stars placeholder with a cinematic 3-layer starfield (procedural shader background + 15K instanced mid-field stars + near sparkle dust) and swap linear fog for FogExp2 scaled to the 300-unit world radius. These are the visual foundation for the space environment — everything else (nebula, lanes) layers on top.

Output: ObservatoryStarfield component, Star Nest GLSL shader, updated ObservatoryWorldScene with FogExp2
</objective>

<execution_context>
@/Users/connor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-space-environment-art/22-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From observatory-world-template.ts:
```typescript
export const WORLD_RADIUS = 300;
// environmentByMode.atlas and .flow both have:
// backgroundColor: "#03060d" / "#02050c"
// starsRadius: 2400, starsDepth: 800, starsCount: 2400, starsFactor: 6
```

From ObservatoryWorldScene.tsx (current — to be modified):
```typescript
// Line 1: import { Stars } from "@react-three/drei"  // <-- REPLACE
// Line 81-88: <Stars ... />  // <-- REPLACE with <ObservatoryStarfield />
// Line 89: <fog attach="fog" args={[...fogColor, fogNear, fogFar]} />  // <-- REPLACE with FogExp2
```

From deriveObservatoryWorld.ts:
```typescript
export interface ObservatoryEnvironmentRecipe {
  backgroundColor: string;
  fogColor: string;
  fogNear: number;
  fogFar: number;
  starsRadius: number;
  starsDepth: number;
  starsCount: number;
  starsFactor: number;
  // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Star Nest GLSL shader + 3-layer ObservatoryStarfield component</name>
  <files>
    apps/workbench/src/features/observatory/shaders/starNest.glsl,
    apps/workbench/src/features/observatory/components/world-canvas/ObservatoryStarfield.tsx,
    apps/workbench/src/features/observatory/__tests__/observatory-starfield.test.tsx
  </files>
  <read_first>
    apps/workbench/src/features/observatory/components/world-canvas/ObservatoryWorldScene.tsx,
    apps/workbench/src/features/observatory/world/observatory-world-template.ts
  </read_first>
  <behavior>
    - Test 1: ObservatoryStarfield exports a named React component
    - Test 2: Component renders without crashing (smoke test with R3F test renderer)
    - Test 3: The GLSL shader file exists and contains "volsteps" (Star Nest signature)
  </behavior>
  <action>
    1. Create `apps/workbench/src/features/observatory/shaders/starNest.glsl` — port the Star Nest shader from Shadertoy (XlfGRj). Fragment shader with uniforms: `time` (float), `resolution` (vec2). Key parameters: volsteps=15, stepsize=0.12, brightness=0.003, saturation=0.65. The shader produces volumetric fractal stars with natural nebula-like color. Keep it subtle (low brightness, moderate saturation) per user decision. ~65 lines GLSL.

    2. Create `apps/workbench/src/features/observatory/components/world-canvas/ObservatoryStarfield.tsx`:
       - **Far layer**: SphereGeometry(2000, 32, 32) with ShaderMaterial using starNest.glsl. Props: `side: THREE.BackSide`, `depthWrite: false`, `depthTest: false`. Animate `time` uniform via useFrame. Import the .glsl file as raw string (Vite handles `?raw` imports, or use inline template literal).
       - **Mid-field stars**: InstancedMesh with 15K instances. Use dual-hemisphere technique: generate 2x7500 points distributed on upper and lower hemispheres at radius 800-1600 from origin. Pre-compute all instance matrices at init (useMemo). Each star is a small PlaneGeometry(0.3, 0.3) with MeshBasicMaterial({color: white, transparent: true, opacity: 0.7, depthWrite: false}). Vertex twinkling: use a custom ShaderMaterial that adds `sin(time * 1.5 + float(gl_InstanceID) * 0.3) * 0.3 + 0.7` alpha modulation, or simpler: just set varying opacity on the MeshBasicMaterial and use a useFrame loop to rotate the entire InstancedMesh very slowly (0.0001 rad/frame) for subtle motion.
       - **Near dust**: `<Sparkles count={1500} size={0.5} speed={0.1} opacity={0.5} color="#aaccff" scale={[200, 200, 200]} />` from drei. This naturally parallaxes during flight.
       - Export as named `ObservatoryStarfield`.

    3. Create test file `apps/workbench/src/features/observatory/__tests__/observatory-starfield.test.tsx`:
       - Smoke test that ObservatoryStarfield renders in a test R3F canvas
       - Verify the component is a valid export

    Note: Using GLSL ShaderMaterial (not TSL) per user decision — Shadertoy port is straightforward. Vite supports GLSL import via inline string or ?raw suffix.
  </action>
  <verify>
    <automated>cd apps/workbench && npx vitest run src/features/observatory/__tests__/observatory-starfield.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "ObservatoryStarfield" apps/workbench/src/features/observatory/components/world-canvas/ObservatoryStarfield.tsx
    - grep -q "volsteps" apps/workbench/src/features/observatory/shaders/starNest.glsl
    - grep -q "InstancedMesh" apps/workbench/src/features/observatory/components/world-canvas/ObservatoryStarfield.tsx
    - grep -q "Sparkles" apps/workbench/src/features/observatory/components/world-canvas/ObservatoryStarfield.tsx
    - grep -q "depthWrite.*false" apps/workbench/src/features/observatory/components/world-canvas/ObservatoryStarfield.tsx
  </acceptance_criteria>
  <done>
    Star Nest GLSL shader renders a subtle volumetric star background on a BackSide sphere; 15K instanced mid-field stars twinkle at mid-distance; drei Sparkles provide near-dust parallax; all layers use depthWrite:false so they render behind scene geometry
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire ObservatoryStarfield into scene + replace linear fog with FogExp2</name>
  <files>
    apps/workbench/src/features/observatory/components/world-canvas/ObservatoryWorldScene.tsx
  </files>
  <read_first>
    apps/workbench/src/features/observatory/components/world-canvas/ObservatoryWorldScene.tsx,
    apps/workbench/src/features/observatory/components/world-canvas/ObservatoryStarfield.tsx
  </read_first>
  <action>
    1. In ObservatoryWorldScene.tsx:
       - Remove import of `Stars` from `@react-three/drei` (clean up the import line — keep other drei imports: CameraShake, OrbitControls)
       - Add import: `import { ObservatoryStarfield } from "./ObservatoryStarfield";`
       - Replace the `<Stars radius={...} depth={...} count={...} factor={...} fade speed={0.4} />` JSX (lines ~81-88) with `<ObservatoryStarfield />`
       - Replace the linear `<fog attach="fog" args={[world.environment.fogColor, world.environment.fogNear, world.environment.fogFar]} />` (line ~89) with:
         ```tsx
         <fogExp2 attach="fog" args={["#060a14", 0.0008]} />
         ```
         Fog color #060a14 (dark navy) matches environment recipe backgroundColor. Density 0.0008 means stations visible at ~500 units, geometry fades at ~800+. The star background sphere is exempt because it has depthWrite:false.

    2. Verify the `<color attach="background" args={["#04080f"]} />` stays as-is (compatible dark background).

    3. Run the full observatory test suite to ensure no regressions.
  </action>
  <verify>
    <automated>cd apps/workbench && npx vitest run src/features/observatory/__tests__/ --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "ObservatoryStarfield" apps/workbench/src/features/observatory/components/world-canvas/ObservatoryWorldScene.tsx
    - grep -q "fogExp2" apps/workbench/src/features/observatory/components/world-canvas/ObservatoryWorldScene.tsx
    - grep -q "0.0008" apps/workbench/src/features/observatory/components/world-canvas/ObservatoryWorldScene.tsx
    - grep -v "drei.*Stars" apps/workbench/src/features/observatory/components/world-canvas/ObservatoryWorldScene.tsx | grep -qv "import.*Stars.*from.*drei" || true
  </acceptance_criteria>
  <done>
    ObservatoryWorldScene renders 3-layer starfield (Star Nest shader + InstancedMesh + Sparkles) instead of drei Stars; FogExp2(0.0008) replaces linear fog; all existing observatory tests pass
  </done>
</task>

</tasks>

<verification>
1. `cd apps/workbench && npx vitest run src/features/observatory/__tests__/ --reporter=verbose` — all tests pass
2. `npx tsc --noEmit` in apps/workbench — no TypeScript errors
3. Star Nest shader file contains volumetric star GLSL code
4. ObservatoryStarfield component exports correctly
5. ObservatoryWorldScene imports ObservatoryStarfield and fogExp2
</verification>

<success_criteria>
- Three star layers exist in ObservatoryStarfield: ShaderMaterial sphere (far), InstancedMesh 15K (mid), Sparkles (near)
- Star Nest GLSL shader renders behind all scene geometry via depthWrite:false + BackSide
- FogExp2 with density 0.0008 replaces linear fog
- All existing observatory tests pass without regression
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/22-space-environment-art/22-01-SUMMARY.md`
</output>
