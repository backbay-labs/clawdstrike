---
phase: 23-station-detail-docking
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/workbench/src/features/observatory/world/StationDockingRing.tsx
  - apps/workbench/src/features/observatory/world/StationLodWrapper.tsx
autonomous: true
requirements: [STN-05]

must_haves:
  truths:
    - "Each station has a visible docking ring (torus) oriented to face outward"
    - "4 guide lights at cardinal positions on the ring pulse when ship is within 50 units"
    - "The docking ring rotates slowly (0.3 rad/s)"
  artifacts:
    - path: "apps/workbench/src/features/observatory/world/StationDockingRing.tsx"
      provides: "Docking ring torus + 4 guide point lights with proximity pulse"
      exports: ["StationDockingRing"]
  key_links:
    - from: "StationLodWrapper.tsx"
      to: "StationDockingRing.tsx"
      via: "near LOD tier child (alongside SpaceStationMesh + FresnelGlow)"
      pattern: "StationDockingRing"
---

<objective>
Add docking ring geometry to each station — a glowing torus with 4 flanking guide lights that pulse on ship approach.

Purpose: The docking ring is the visual landing approach guide. It tells pilots exactly where to aim and provides feedback as they close in.
Output: StationDockingRing component; StationLodWrapper near-tier updated to include it.
</objective>

<execution_context>
@/Users/connor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@apps/workbench/src/features/observatory/world/districtGeometry.tsx
@apps/workbench/src/features/observatory/stores/observatory-store.ts
@apps/workbench/src/features/observatory/character/ship/flight-types.ts

<interfaces>
<!-- Key types the executor needs. -->

From flight-types.ts:
```typescript
export interface FlightState {
  position: [number, number, number];
  nearestStationId: HuntStationId | null;
  currentSpeed: number;
  speedTier: SpeedTier;
}
export const DEFAULT_FLIGHT_CONFIG: FlightConfig = {
  dockProximityRadius: 50,
  dockSpeed: 8,
};
```

From observatory-store.ts:
```typescript
export const useObservatoryStore = createSelectors(useObservatoryStoreBase);
// Usage: useObservatoryStore.use.flightState()
```

From observatory-world-template.ts:
```typescript
export const OBSERVATORY_STATION_POSITIONS: Record<HuntStationId, ObservatoryVec3>;
```

StationLodWrapper (from Plan 01 — may not exist yet, but structure is):
```typescript
export interface StationLodWrapperProps extends SpaceStationMeshProps {
  stationLabel: string;
}
// Near LOD tier is a <group> containing SpaceStationMesh + StationFresnelGlow
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: StationDockingRing component</name>
  <read_first>
    - apps/workbench/src/features/observatory/world/districtGeometry.tsx
    - apps/workbench/src/features/observatory/character/ship/flight-types.ts
    - apps/workbench/src/features/observatory/stores/observatory-store.ts
  </read_first>
  <files>
    apps/workbench/src/features/observatory/world/StationDockingRing.tsx
  </files>
  <action>
    Create `StationDockingRing.tsx`:

    Props: `{ colorHex: string; stationId: HuntStationId }`.

    **Torus ring:**
    - `<mesh rotation={[Math.PI / 2, 0, 0]}>` (ring lies in XZ plane, rotate to face outward)
    - `<torusGeometry args={[6, 0.15, 16, 48]} />` per CONTEXT.md
    - `<meshBasicMaterial color={colorHex} toneMapped={false} transparent opacity={0.8} />` — MeshBasicMaterial so it glows through bloom without needing lights.

    **Rotation animation:**
    - `useFrame((_, delta) => { ringRef.current.rotation.z += delta * 0.3; })` — 0.3 rad/s per CONTEXT.md.
    - Pre-allocate ref: `const ringRef = useRef<THREE.Mesh>(null)`.

    **4 guide lights:**
    - 4 `<pointLight>` at cardinal positions on the ring: `[6, 0, 0]`, `[-6, 0, 0]`, `[0, 0, 6]`, `[0, 0, -6]`.
    - Each: `intensity={1.5}`, `distance={8}`, `color={colorHex}`.
    - Store refs to all 4 lights in a single `useRef<(THREE.PointLight | null)[]>([null, null, null, null])`.

    **Proximity pulse:**
    - Read ship position from `useObservatoryStore.use.flightState()` — but to avoid per-frame re-render from store, use `useObservatoryStore.getState().flightState` inside useFrame instead.
    - Actually, read the `flightState` via `useObservatoryStore.getState()` inside useFrame (no subscription, no re-render).
    - Import `OBSERVATORY_STATION_POSITIONS` from `../../world/observatory-world-template`.
    - In useFrame: compute distance from `flightState.position` to this station's position.
    - If distance < 50: pulse guide light intensity via `1.5 + Math.sin(clock.elapsedTime * 3) * 1.0` (faster pulse when close).
    - If distance >= 50: set guide light intensity to `0.3` (dim idle state).
    - Pre-allocate a `THREE.Vector3` at module level for distance computation (zero GC in hot path).
  </action>
  <verify>
    <automated>cd /Users/connor/Medica/backbay/standalone/clawdstrike-worktrees/huntronomer-workbench && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "StationDockingRing" apps/workbench/src/features/observatory/world/StationDockingRing.tsx
    - grep -q "torusGeometry" apps/workbench/src/features/observatory/world/StationDockingRing.tsx
    - grep -q "pointLight" apps/workbench/src/features/observatory/world/StationDockingRing.tsx
    - grep -q "0.3" apps/workbench/src/features/observatory/world/StationDockingRing.tsx
    - grep -q "toneMapped" apps/workbench/src/features/observatory/world/StationDockingRing.tsx
  </acceptance_criteria>
  <done>StationDockingRing renders torus + 4 point lights; ring rotates at 0.3 rad/s; guide lights pulse when ship is within 50 units; TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Mount StationDockingRing in StationLodWrapper near-tier</name>
  <read_first>
    - apps/workbench/src/features/observatory/world/StationLodWrapper.tsx
    - apps/workbench/src/features/observatory/world/StationDockingRing.tsx
  </read_first>
  <files>
    apps/workbench/src/features/observatory/world/StationLodWrapper.tsx
  </files>
  <action>
    In StationLodWrapper.tsx:
    1. Add `stationId: HuntStationId` to the props interface (import HuntStationId from `../world/types` or wherever the type lives in the local module — check the existing imports).
    2. Import `StationDockingRing` from `./StationDockingRing`.
    3. In the near LOD tier `<group>` (first child of `<Detailed>`), add `<StationDockingRing colorHex={colorHex} stationId={stationId} />` alongside SpaceStationMesh and StationFresnelGlow.
    4. Update ObservatoryDistrictLayer.tsx to pass `stationId={district.id}` to StationLodWrapper.

    Note: If Plan 01 has not been executed yet (StationLodWrapper does not exist), this task creates the docking ring component but defers the wiring until Plan 01 lands first. Check if the file exists before modifying. If it does not exist, add a comment to the StationDockingRing file noting it should be mounted in StationLodWrapper's near tier.
  </action>
  <verify>
    <automated>cd /Users/connor/Medica/backbay/standalone/clawdstrike-worktrees/huntronomer-workbench && npx tsc --noEmit 2>&1 | tail -5 && npx vitest run --reporter=verbose apps/workbench/src/features/observatory/__tests__/ 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "StationDockingRing" apps/workbench/src/features/observatory/world/StationLodWrapper.tsx
    - grep -q "stationId" apps/workbench/src/features/observatory/world/StationLodWrapper.tsx
    - grep -q "stationId" apps/workbench/src/features/observatory/components/world-canvas/ObservatoryDistrictLayer.tsx
  </acceptance_criteria>
  <done>StationDockingRing mounted in StationLodWrapper near-tier; stationId prop flows from ObservatoryDistrictLayer through StationLodWrapper to StationDockingRing; TypeScript clean; tests pass.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0
- All observatory tests pass
- StationDockingRing has torusGeometry(6, 0.15, 16, 48), 4 pointLights, 0.3 rad/s rotation
- Guide lights pulse faster (sin * 3) when ship within 50 units
</verification>

<success_criteria>
- Each station at near range shows a docking ring torus rotating at 0.3 rad/s
- 4 guide lights at cardinal positions pulse when ship is within 50 units
- Ring uses MeshBasicMaterial with toneMapped=false for bloom visibility
- No TypeScript errors; no test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/23-station-detail-docking/23-02-SUMMARY.md`
</output>
