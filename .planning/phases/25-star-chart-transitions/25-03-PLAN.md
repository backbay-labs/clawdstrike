---
phase: 25-star-chart-transitions
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/workbench/src/features/observatory/components/ObservatoryWorldCanvas.tsx
  - apps/workbench/src/features/observatory/components/ObservatoryPostFX.tsx
  - apps/workbench/src/features/observatory/vfx/ObservatoryVFXPools.tsx
  - apps/workbench/src/features/observatory/vfx/WarpSpeedLines.tsx
autonomous: true
requirements: [TRN-01, TRN-02, TRN-04]

must_haves:
  truths:
    - "Activating boost animates FOV from 60 to 90 and back with ease-in/ease-out timing"
    - "40 instanced speed-line particles radiate from camera forward during boost duration"
    - "Bloom luminanceThreshold drops from 0.85 to 0.5 during boost and eases back after"
    - "All three effects (FOV + lines + bloom) fire simultaneously on boost activation and resolve before cooldown ends"
  artifacts:
    - path: "apps/workbench/src/features/observatory/components/ObservatoryWorldCanvas.tsx"
      provides: "FovController updated with boost FOV ease-in/ease-out timing"
      contains: "boostFov"
    - path: "apps/workbench/src/features/observatory/vfx/WarpSpeedLines.tsx"
      provides: "Instanced cylinder speed lines relative to camera during boost"
      contains: "WarpSpeedLines"
    - path: "apps/workbench/src/features/observatory/components/ObservatoryPostFX.tsx"
      provides: "Dynamic luminanceThreshold via ref for bloom spike"
      contains: "luminanceThresholdRef\|bloomSpikeRef"
    - path: "apps/workbench/src/features/observatory/vfx/ObservatoryVFXPools.tsx"
      provides: "warp-speed-lines particle pool"
      contains: "warp-speed-lines"
  key_links:
    - from: "FovController"
      to: "observatory-store flightState.speedTier"
      via: "getState() reads speedTier to detect boost"
      pattern: "speedTier.*boost"
    - from: "WarpSpeedLines"
      to: "observatory-store flightState.speedTier"
      via: "reads speedTier to trigger emission"
      pattern: "speedTier.*boost"
    - from: "ObservatoryPostFX"
      to: "ObservatoryWorldCanvas"
      via: "bloomSpikeActive prop or ref"
      pattern: "bloomSpike\|luminanceThreshold"
---

<objective>
Create the dramatic boost transition: simultaneous FOV punch (60->90->60), instanced speed-line particles from camera center, and bloom intensity spike. All three effects fire together when boost activates and resolve before boost cooldown ends.

Purpose: Boost should be the most dramatic moment in the flight experience — the combined visual punch makes it feel powerful and satisfying.
Output: Enhanced FovController with boost easing, WarpSpeedLines component, bloom spike in ObservatoryPostFX.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-star-chart-transitions/25-CONTEXT.md
@apps/workbench/src/features/observatory/components/ObservatoryWorldCanvas.tsx
@apps/workbench/src/features/observatory/components/ObservatoryPostFX.tsx
@apps/workbench/src/features/observatory/vfx/ObservatoryVFXPools.tsx
@apps/workbench/src/features/observatory/character/ship/flight-types.ts
@apps/workbench/src/features/observatory/stores/observatory-store.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From ObservatoryWorldCanvas.tsx — existing FovController:
```typescript
export function FovController({
  playerFocusRef,
  probeActive,
}: {
  playerFocusRef: RefObject<ObservatoryPlayerFocusState | null>;
  probeActive: boolean;
}) {
  useFrame(({ camera }, delta) => {
    const pCam = camera as THREE.PerspectiveCamera;
    const safeDelta = Math.min(delta, 1 / 20);
    const sprinting = playerFocusRef.current?.sprinting ?? false;
    const targetFov = probeActive ? 35 : sprinting ? 90 : 60;
    pCam.fov += (targetFov - pCam.fov) * lerpAlpha(5.0, safeDelta);
    pCam.updateProjectionMatrix();
  });
  return null;
}
```

From ObservatoryPostFX.tsx:
```typescript
export interface ObservatoryPostFXProps {
  activeHeroPropPosition?: [number, number, number] | null;
  spiritLut?: THREE.Data3DTexture | null;
  profile: Pick<ObservatoryPerformanceProfile, ...>;
}
// Bloom element: luminanceThreshold={0.85}
```

From flight-types.ts:
```typescript
export type SpeedTier = "cruise" | "boost" | "dock";
export interface FlightState {
  speedTier: SpeedTier;
  boostActivatedAtMs: number | null;
  // ...
}
export const DEFAULT_FLIGHT_CONFIG = {
  boostDurationMs: 2000,
  boostCooldownMs: 4000,
};
```

From ObservatoryVFXPools.tsx:
```typescript
// Existing pools: landing-dust (200), thruster-exhaust (300),
// ship-thruster-exhaust (400), lane-particle-stream (600)
```

wawa-vfx pattern:
```typescript
import { VFXParticles, VFXEmitter, RenderMode } from "wawa-vfx";
// VFXParticles = pool declaration (name, settings)
// VFXEmitter = emitter that targets a named pool
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Boost FOV punch + bloom spike</name>
  <read_first>
    - apps/workbench/src/features/observatory/components/ObservatoryWorldCanvas.tsx (FovController function, ~line 195-211, and where it is mounted ~line 3811)
    - apps/workbench/src/features/observatory/components/ObservatoryPostFX.tsx
    - apps/workbench/src/features/observatory/stores/observatory-store.ts (flightState fields)
    - apps/workbench/src/features/observatory/character/ship/flight-types.ts (SpeedTier, boostDurationMs)
  </read_first>
  <files>
    apps/workbench/src/features/observatory/components/ObservatoryWorldCanvas.tsx
    apps/workbench/src/features/observatory/components/ObservatoryPostFX.tsx
  </files>
  <action>
    **FovController boost FOV punch (TRN-01):**
    - The existing FovController already targets FOV=90 when `sprinting` is true. But in the ship flight mode, boost is detected via `speedTier === "boost"` from the observatory store, not `playerFocusRef.sprinting`.
    - Modify FovController to also read the store's flightState.speedTier via `useObservatoryStore.getState().flightState.speedTier`.
    - When speedTier === "boost", target FOV = 90 (same as sprint).
    - For the easing per CONTEXT.md: FOV 60->90 over 0.3s (ease-in), then 90->60 over 0.8s (ease-out).
    - Implement with a boost phase tracker:
      - Add a `boostFovPhaseRef = useRef<"idle" | "ramp-up" | "sustain" | "ramp-down">("idle")` and a `boostFovTimerRef = useRef(0)`.
      - On boost start (speedTier transitions to "boost"): set phase = "ramp-up", timer = 0.
      - "ramp-up": lerp FOV from 60 to 90 over 0.3s using timer. easeIn: t*t. When timer >= 0.3s -> "sustain".
      - "sustain": hold FOV at 90. When speedTier leaves "boost" -> "ramp-down", timer = 0.
      - "ramp-down": lerp FOV from 90 to 60 over 0.8s. easeOut: 1-(1-t)*(1-t). When timer >= 0.8s -> "idle".
      - "idle": use existing targetFov logic (probe=35, sprint=90, default=60).
    - Track previous speedTier in a ref to detect transitions.

    **Bloom spike (TRN-04):**
    - ObservatoryPostFX currently has `luminanceThreshold={0.85}` hardcoded in the Bloom element.
    - Add a new optional prop: `bloomLuminanceOverride?: number | null`.
    - When provided (non-null), use it instead of 0.85 for the Bloom luminanceThreshold.
    - In ObservatoryWorldCanvas, manage the bloom spike state:
      - Add a `bloomSpikeRef = useRef<number | null>(null)` and a `bloomSpikeTimerRef = useRef(0)`.
      - When boost activates (speedTier transitions to "boost"): set bloomSpikeRef.current = 0.5, timer = 0.
      - Over 0.8s, hold at 0.5. Then ease back to 0.85 over 0.5s: `0.5 + 0.35 * easeOut(t)`.
      - When timer exceeds 1.3s total: set bloomSpikeRef.current = null.
      - Pass bloomSpikeRef.current as the `bloomLuminanceOverride` prop to LazyObservatoryPostFX.
    - Since ObservatoryPostFX uses Bloom as a JSX element, the threshold needs to be reactive. Use a state variable (`bloomThreshold`) updated from a useFrame inside the canvas (or pass as prop from the canvas component that already runs useFrame logic).
    - Simpler approach: Add a small `BloomSpikeController` component inside the Canvas that uses useFrame to update a shared ref, and ObservatoryPostFX reads from that ref.
    - Actually simplest: Add `bloomLuminanceOverride` prop to ObservatoryPostFX. The parent (ObservatoryWorldCanvas) manages a `const [bloomThreshold, setBloomThreshold] = useState<number | null>(null)` and a useEffect that reacts to speedTier changes. Since this is a rare event (boost activation), a single setState is acceptable (not 60fps).

    **Integration in ObservatoryWorldCanvas:**
    - Where FovController is mounted (~line 3811), the boost detection already works because the playerFocusRef.current?.sprinting maps to the character controller sprint. For ship flight, add a new prop to FovController: `boostActive: boolean`.
    - Read speedTier from store: `const speedTier = useObservatoryStore((s) => s.flightState.speedTier);`
    - Pass `boostActive={speedTier === "boost"}` to FovController.
    - Pass `bloomLuminanceOverride={bloomThreshold}` to LazyObservatoryPostFX.
    - Manage bloomThreshold via useEffect on speedTier: when "boost", set to 0.5, then setTimeout 800ms to start easing back (set to null after 1300ms total). Or handle entirely in a useFrame inside a small controller component.
  </action>
  <verify>
    <automated>cd apps/workbench && npx vitest run src/features/observatory/__tests__/observatory-tab.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "boostActive\|boost.*fov\|boostFov" apps/workbench/src/features/observatory/components/ObservatoryWorldCanvas.tsx
    - grep -q "bloomLuminanceOverride\|luminanceOverride" apps/workbench/src/features/observatory/components/ObservatoryPostFX.tsx
    - grep -q "speedTier" apps/workbench/src/features/observatory/components/ObservatoryWorldCanvas.tsx
  </acceptance_criteria>
  <done>Boost activation triggers FOV 60->90 over 0.3s (ease-in), holds during boost, eases back 90->60 over 0.8s. Bloom luminanceThreshold drops from 0.85 to 0.5 for 0.8s and eases back over 0.5s. Both effects fire on boost activation and resolve before cooldown ends (2s boost + 4s cooldown = 6s total).</done>
</task>

<task type="auto">
  <name>Task 2: Warp speed line particles</name>
  <read_first>
    - apps/workbench/src/features/observatory/vfx/ObservatoryVFXPools.tsx
    - apps/workbench/src/features/observatory/vfx/CharacterVFX.tsx (for wawa-vfx emission pattern)
    - apps/workbench/src/features/observatory/components/ObservatoryWorldCanvas.tsx (where VFX pools are mounted)
    - apps/workbench/src/features/observatory/stores/observatory-store.ts (flightState.speedTier)
  </read_first>
  <files>
    apps/workbench/src/features/observatory/vfx/ObservatoryVFXPools.tsx
    apps/workbench/src/features/observatory/vfx/WarpSpeedLines.tsx
  </files>
  <action>
    **VFX Pool (TRN-02):**
    - Add a "warp-speed-lines" pool to ObservatoryVFXPools:
      ```
      <VFXParticles
        name="warp-speed-lines"
        settings={{
          nbParticles: 80,
          renderMode: RenderMode.StretchBillboard,
          gravity: [0, 0, 0],
          fadeAlpha: [0.0, 0.15],
        }}
      />
      ```
    - 80 particles (40 active + 40 buffer for continuous emission during 2s boost).

    **WarpSpeedLines component (new file):**
    - Create `apps/workbench/src/features/observatory/vfx/WarpSpeedLines.tsx`.
    - Component receives `active: boolean` prop (true when speedTier === "boost").
    - Uses VFXEmitter from wawa-vfx targeting "warp-speed-lines" pool.
    - Positioning: Attach to the camera using `useFrame` to update the emitter's position to the camera's world position.
    - Particle arrangement: 40 thin cylinders arranged in a cone pattern around the camera forward vector.
    - Each frame while active: emit particles at positions in a ring around the camera forward axis, offset 2-4 units ahead of the camera, with random polar angle (0.1 to 0.3 radians from center) and random azimuthal angle (0 to 2*PI).
    - Particle velocity: along the camera forward vector (negative Z in camera space transformed to world), speed 8-12.
    - Particle settings per emission:
      - lifetime: [0.3, 0.5]
      - size: [0.02, 0.04] (thin)
      - color: white-blue [#b8d4ff to #ffffff]
      - stretchBillboard already handled by pool renderMode
    - Use `toneMapped={false}` on the VFXParticles material so lines bloom.
    - When `active` becomes false, stop emitting (existing particles fade out naturally via pool fadeAlpha).

    **Mount WarpSpeedLines:**
    - The component should be mounted inside the Canvas context in ObservatoryWorldCanvas, near where existing VFX components are mounted. Wrap in Suspense if lazy-loaded.
    - Pass `active={speedTier === "boost"}` where speedTier is already read from the store in Task 1.
    - If the component is simple enough (< 50 lines), mount it directly rather than lazy-loading.

    NOTE: If wawa-vfx VFXEmitter API does not support per-frame imperative emission easily, fall back to a simpler InstancedMesh approach:
    - Create an InstancedMesh with 40 instances of thin CylinderGeometry (radius 0.02, height 3-6).
    - In useFrame, when active, update instance matrices to form a cone around camera forward.
    - Material: MeshBasicMaterial color="#b8d4ff" toneMapped={false} transparent opacity={0.6}.
    - When !active, set all instance scales to 0.
  </action>
  <verify>
    <automated>cd apps/workbench && npx vitest run src/features/observatory/__tests__/observatory-tab.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "warp-speed-lines" apps/workbench/src/features/observatory/vfx/ObservatoryVFXPools.tsx
    - test -f apps/workbench/src/features/observatory/vfx/WarpSpeedLines.tsx
    - grep -q "WarpSpeedLines\|warpSpeedLines" apps/workbench/src/features/observatory/vfx/WarpSpeedLines.tsx
    - grep -q "toneMapped.*false\|toneMapped={false}" apps/workbench/src/features/observatory/vfx/WarpSpeedLines.tsx
  </acceptance_criteria>
  <done>40 instanced speed-line particles radiate from camera forward during boost, visible through bloom, fade out naturally when boost ends. "warp-speed-lines" pool added to ObservatoryVFXPools. WarpSpeedLines mounted in ObservatoryWorldCanvas gated by speedTier==="boost".</done>
</task>

</tasks>

<verification>
- Boost activation simultaneously triggers: FOV punch 60->90, speed line particles, bloom spike
- FOV ease-in 0.3s, ease-out 0.8s
- Speed lines visible for boost duration (~2s), fade on boost end
- Bloom threshold drops to 0.5 for 0.8s, eases back over 0.5s
- All effects resolve before boost cooldown ends
- Non-boost flight: FOV remains at 60, no speed lines, normal bloom threshold
- All existing observatory tests pass
</verification>

<success_criteria>
- Boost is the most visually dramatic moment: FOV + lines + bloom spike fire together
- Speed lines are thin white-blue streaks that bloom due to toneMapped=false
- Effects are time-bounded and do not persist beyond boost duration
- No per-frame setState in any effect (refs and useFrame only for animation)
- All tests pass: `cd apps/workbench && npx vitest run src/features/observatory/__tests__/`
</success_criteria>

<output>
After completion, create `.planning/phases/25-star-chart-transitions/25-03-SUMMARY.md`
</output>
