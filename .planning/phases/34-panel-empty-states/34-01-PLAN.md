---
phase: 34-panel-empty-states
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/workbench/src/features/observatory/components/hud/panels/ExplainabilityDrawerPanel.tsx
  - apps/workbench/src/features/observatory/components/hud/panels/MissionDrawerPanel.tsx
  - apps/workbench/src/features/observatory/components/hud/panels/GhostMemoryDrawerPanel.tsx
  - apps/workbench/src/features/observatory/__tests__/observatory-drawer-panels.test.tsx
autonomous: true
requirements:
  - EMP-01
  - EMP-02
  - EMP-03

must_haves:
  truths:
    - "Explainability panel empty state shows section headers STATION, PRESSURE LANES, ANOMALIES with muted text and a hint about clicking a station or pressing E"
    - "Mission panel empty state shows section outline BRIEFING, OBJECTIVES, NARRATIVE with muted labels and a hint about the command palette"
    - "Ghost Memory panel empty state shows 0 traces header with a muted explanation paragraph describing what ghost memory records and when traces appear"
  artifacts:
    - path: "apps/workbench/src/features/observatory/components/hud/panels/ExplainabilityDrawerPanel.tsx"
      provides: "Structured empty state with 3 section headers and hint text"
      contains: "STATION"
    - path: "apps/workbench/src/features/observatory/components/hud/panels/MissionDrawerPanel.tsx"
      provides: "Structured empty state with 3 section headers and command palette hint"
      contains: "BRIEFING"
    - path: "apps/workbench/src/features/observatory/components/hud/panels/GhostMemoryDrawerPanel.tsx"
      provides: "Explanatory empty state with description of ghost memory purpose"
      contains: "prior hunt findings"
  key_links:
    - from: "apps/workbench/src/features/observatory/__tests__/observatory-drawer-panels.test.tsx"
      to: "ExplainabilityDrawerPanel.tsx"
      via: "render + querySelector for section headers"
      pattern: "STATION.*PRESSURE.*ANOMALIES"
    - from: "apps/workbench/src/features/observatory/__tests__/observatory-drawer-panels.test.tsx"
      to: "MissionDrawerPanel.tsx"
      via: "render + querySelector for section headers"
      pattern: "BRIEFING.*OBJECTIVES.*NARRATIVE"
    - from: "apps/workbench/src/features/observatory/__tests__/observatory-drawer-panels.test.tsx"
      to: "GhostMemoryDrawerPanel.tsx"
      via: "render + textContent check for explanation text"
      pattern: "prior hunt findings"
---

<objective>
Replace bare single-line empty states in Explainability, Mission, and Ghost Memory panels with structured placeholder content that previews the panel's section layout and gives operators contextual hints.

Purpose: Operators opening these panels with no data see a structured preview of what the panel will contain, not a blank centered italic message. This makes the UI feel production-ready from the first frame.
Output: 3 modified panel components + extended test coverage
</objective>

<execution_context>
@/Users/connor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-panel-empty-states/34-CONTEXT.md

@apps/workbench/src/features/observatory/components/hud/panels/ExplainabilityDrawerPanel.tsx
@apps/workbench/src/features/observatory/components/hud/panels/MissionDrawerPanel.tsx
@apps/workbench/src/features/observatory/components/hud/panels/GhostMemoryDrawerPanel.tsx
@apps/workbench/src/features/observatory/__tests__/observatory-drawer-panels.test.tsx
@apps/workbench/src/features/observatory/observatory-hud.css

<interfaces>
<!-- Existing styling patterns the executor must follow -->

From observatory-hud.css:
```css
--hud-text: rgba(255, 255, 255, 0.85);
--hud-text-muted: rgba(255, 255, 255, 0.45);
--hud-accent: var(--spirit-accent, #4af);
```

From ExplainabilityDrawerPanel.tsx (shared section heading pattern):
```typescript
const sectionHeadingStyle: React.CSSProperties = {
  fontSize: 10,
  textTransform: "uppercase",
  letterSpacing: "0.12em",
  color: "var(--hud-text-muted)",
  marginBottom: 8,
  marginTop: 0,
};
```

Existing data-testid conventions:
- `data-testid="explainability-empty-state"` (on the empty state root div)
- `data-testid="mission-empty-state"` (on the empty state root div)
- `data-testid="ghost-memory-empty-state"` (on the empty state root div)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace bare empty states with structured placeholder content in all 3 panels</name>
  <files>
    apps/workbench/src/features/observatory/components/hud/panels/ExplainabilityDrawerPanel.tsx
    apps/workbench/src/features/observatory/components/hud/panels/MissionDrawerPanel.tsx
    apps/workbench/src/features/observatory/components/hud/panels/GhostMemoryDrawerPanel.tsx
    apps/workbench/src/features/observatory/__tests__/observatory-drawer-panels.test.tsx
  </files>
  <read_first>
    apps/workbench/src/features/observatory/components/hud/panels/ExplainabilityDrawerPanel.tsx
    apps/workbench/src/features/observatory/components/hud/panels/MissionDrawerPanel.tsx
    apps/workbench/src/features/observatory/components/hud/panels/GhostMemoryDrawerPanel.tsx
    apps/workbench/src/features/observatory/__tests__/observatory-drawer-panels.test.tsx
  </read_first>
  <behavior>
    - Test: ExplainabilityDrawerPanel empty state contains text "STATION" (section header, via textContent.toUpperCase() since CSS textTransform is visual-only in jsdom — but the literal text should be uppercase in JSX)
    - Test: ExplainabilityDrawerPanel empty state contains text "PRESSURE" (section header for pressure lanes)
    - Test: ExplainabilityDrawerPanel empty state contains text "ANOMALIES" (section header)
    - Test: ExplainabilityDrawerPanel empty state contains hint text "Click a station or press E while hovering"
    - Test: MissionDrawerPanel empty state contains text "BRIEFING" (section header)
    - Test: MissionDrawerPanel empty state contains text "OBJECTIVES" (section header)
    - Test: MissionDrawerPanel empty state contains text "NARRATIVE" (section header)
    - Test: MissionDrawerPanel empty state contains hint text "Start a mission from the command palette"
    - Test: GhostMemoryDrawerPanel empty state contains "0 traces" (already passes — keep passing)
    - Test: GhostMemoryDrawerPanel empty state contains explanatory text about "prior hunt findings"
    - Test: GhostMemoryDrawerPanel empty state contains text about when traces appear: "probing stations"
  </behavior>
  <action>
    **Step 1: Write failing tests first.** Add new test cases to observatory-drawer-panels.test.tsx:

    In the ExplainabilityDrawerPanel describe block, add a new test:
    ```
    it("e. empty state shows structured section headers and hint", () => {
      const { container } = render(<ExplainabilityDrawerPanel />);
      const el = container.querySelector("[data-testid='explainability-empty-state']");
      expect(el?.textContent).toContain("STATION");
      expect(el?.textContent).toContain("PRESSURE");
      expect(el?.textContent).toContain("ANOMALIES");
      expect(el?.textContent).toContain("Click a station or press E while hovering");
    });
    ```

    In the MissionDrawerPanel describe block, add a new test:
    ```
    it("e. empty state shows structured section headers and hint", () => {
      const { container } = render(<MissionDrawerPanel />);
      const el = container.querySelector("[data-testid='mission-empty-state']");
      expect(el?.textContent).toContain("BRIEFING");
      expect(el?.textContent).toContain("OBJECTIVES");
      expect(el?.textContent).toContain("NARRATIVE");
      expect(el?.textContent).toContain("Start a mission from the command palette");
    });
    ```

    In the GhostMemoryDrawerPanel describe block, add a new test:
    ```
    it("e. empty state shows explanatory description of ghost memory", () => {
      const { container } = render(<GhostMemoryDrawerPanel />);
      const el = container.querySelector("[data-testid='ghost-memory-empty-state']");
      expect(el?.textContent).toContain("prior hunt findings");
      expect(el?.textContent).toContain("probing stations");
    });
    ```

    Run tests — all 3 new tests must FAIL (RED).

    **Step 2: Implement structured empty states (GREEN).**

    **ExplainabilityDrawerPanel.tsx** — Replace the bare empty state (lines 43-61, the `if (selectedStationId === null)` return block) with a structured layout. Keep `data-testid="explainability-empty-state"` on the root div. Change from centered flex to top-aligned column layout matching the populated panel's structure:

    ```tsx
    if (selectedStationId === null) {
      return (
        <div
          data-testid="explainability-empty-state"
          style={{
            display: "flex",
            flexDirection: "column",
            gap: 16,
            height: "100%",
            fontFamily: "inherit",
            padding: 0,
          }}
        >
          {/* Station section placeholder */}
          <div
            style={{
              borderLeft: "4px solid rgba(255,255,255,0.08)",
              paddingLeft: 12,
              paddingTop: 4,
              paddingBottom: 4,
            }}
          >
            <div style={{ ...sectionHeadingStyle, marginBottom: 4 }}>STATION</div>
            <div style={{ fontSize: 12, color: "var(--hud-text-muted)", fontStyle: "italic" }}>
              No station selected
            </div>
          </div>

          {/* Pressure section placeholder */}
          <div>
            <div style={sectionHeadingStyle}>PRESSURE LANES</div>
            <div style={{ display: "flex", flexDirection: "column", gap: 6 }}>
              {[1, 2, 3].map((i) => (
                <div key={i} style={{ display: "flex", alignItems: "center", gap: 8 }}>
                  <span style={{ fontSize: 11, color: "var(--hud-text-muted)", width: 20, textAlign: "right" }}>{i}</span>
                  <span style={{ flex: 1, height: 8, borderRadius: 4, background: "rgba(255,255,255,0.04)" }} />
                  <div style={{ width: 48, height: 3, borderRadius: 2, background: "rgba(255,255,255,0.04)" }} />
                </div>
              ))}
            </div>
          </div>

          {/* Anomalies section placeholder */}
          <div>
            <div style={sectionHeadingStyle}>ANOMALIES</div>
            <div style={{ fontSize: 12, color: "var(--hud-text-muted)", fontStyle: "italic" }}>
              No anomalies to display
            </div>
          </div>

          {/* Spacer */}
          <div style={{ flex: 1 }} />

          {/* Hint */}
          <div style={{
            fontSize: 12,
            color: "var(--hud-text-muted)",
            textAlign: "center",
            padding: "12px 8px",
            borderTop: "1px solid rgba(255,255,255,0.04)",
            lineHeight: 1.5,
          }}>
            Click a station or press E while hovering
          </div>
        </div>
      );
    }
    ```

    **MissionDrawerPanel.tsx** — Replace the bare empty state (lines 37-55, the `if (mission === null)` return block) with a structured layout. Keep `data-testid="mission-empty-state"`:

    ```tsx
    if (mission === null) {
      return (
        <div
          data-testid="mission-empty-state"
          style={{
            display: "flex",
            flexDirection: "column",
            gap: 16,
            height: "100%",
            fontFamily: "inherit",
            padding: 0,
          }}
        >
          {/* Briefing section placeholder */}
          <div>
            <div style={sectionHeadingStyle}>BRIEFING</div>
            <div style={{
              fontSize: 12,
              color: "var(--hud-text-muted)",
              fontStyle: "italic",
              lineHeight: 1.5,
            }}>
              No mission briefing available
            </div>
          </div>

          {/* Objectives section placeholder */}
          <div>
            <div style={sectionHeadingStyle}>OBJECTIVES</div>
            <div style={{ display: "flex", flexDirection: "column", gap: 8 }}>
              {[1, 2, 3].map((i) => (
                <div key={i} style={{ display: "flex", gap: 8, alignItems: "center" }}>
                  <span style={{ fontSize: 14, color: "var(--hud-text-muted)" }}>{"\u25CB"}</span>
                  <span style={{ flex: 1, height: 8, borderRadius: 4, background: "rgba(255,255,255,0.04)" }} />
                </div>
              ))}
            </div>
          </div>

          {/* Narrative section placeholder */}
          <div>
            <div style={sectionHeadingStyle}>NARRATIVE</div>
            <div style={{
              fontSize: 12,
              color: "var(--hud-text-muted)",
              fontStyle: "italic",
              lineHeight: 1.5,
            }}>
              Mission narrative will appear here
            </div>
          </div>

          {/* Spacer */}
          <div style={{ flex: 1 }} />

          {/* Hint */}
          <div style={{
            fontSize: 12,
            color: "var(--hud-text-muted)",
            textAlign: "center",
            padding: "12px 8px",
            borderTop: "1px solid rgba(255,255,255,0.04)",
            lineHeight: 1.5,
          }}>
            Start a mission from the command palette
          </div>
        </div>
      );
    }
    ```

    **GhostMemoryDrawerPanel.tsx** — Modify the empty state block (lines 93-109, the `traces.length === 0` branch). Keep the existing `data-testid="ghost-memory-empty-state"` and the "0 traces" header that already renders above. Replace the bare "No prior findings or receipts" text with an explanatory paragraph:

    ```tsx
    {traces.length === 0 ? (
      <div
        data-testid="ghost-memory-empty-state"
        style={{
          display: "flex",
          flexDirection: "column",
          gap: 12,
          padding: "0 0",
          flex: 1,
        }}
      >
        <div style={{
          fontSize: 12,
          color: "var(--hud-text-muted)",
          lineHeight: 1.6,
        }}>
          Ghost memory records prior hunt findings, receipt verdicts, and case-note traces
          from your observatory sessions. Traces serve as persistent breadcrumbs that help
          you retrace investigative paths across hunts.
        </div>
        <div style={{
          fontSize: 12,
          color: "var(--hud-text-muted)",
          lineHeight: 1.6,
        }}>
          Traces appear after probing stations, completing mission objectives, or generating
          receipts during analysis.
        </div>
      </div>
    ) : ( ... )}
    ```

    Run tests — all tests must PASS (GREEN).

    **Step 3:** Verify the original tests (a-d in each describe block) still pass unchanged. The existing `data-testid` attributes and populated-state rendering are untouched.
  </action>
  <verify>
    <automated>cd apps/workbench && npx vitest run src/features/observatory/__tests__/observatory-drawer-panels.test.tsx --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "STATION" apps/workbench/src/features/observatory/components/hud/panels/ExplainabilityDrawerPanel.tsx
    - grep -q "PRESSURE" apps/workbench/src/features/observatory/components/hud/panels/ExplainabilityDrawerPanel.tsx
    - grep -q "ANOMALIES" apps/workbench/src/features/observatory/components/hud/panels/ExplainabilityDrawerPanel.tsx
    - grep -q "Click a station or press E while hovering" apps/workbench/src/features/observatory/components/hud/panels/ExplainabilityDrawerPanel.tsx
    - grep -q "BRIEFING" apps/workbench/src/features/observatory/components/hud/panels/MissionDrawerPanel.tsx
    - grep -q "OBJECTIVES" apps/workbench/src/features/observatory/components/hud/panels/MissionDrawerPanel.tsx
    - grep -q "NARRATIVE" apps/workbench/src/features/observatory/components/hud/panels/MissionDrawerPanel.tsx
    - grep -q "Start a mission from the command palette" apps/workbench/src/features/observatory/components/hud/panels/MissionDrawerPanel.tsx
    - grep -q "prior hunt findings" apps/workbench/src/features/observatory/components/hud/panels/GhostMemoryDrawerPanel.tsx
    - grep -q "probing stations" apps/workbench/src/features/observatory/components/hud/panels/GhostMemoryDrawerPanel.tsx
    - grep -q "empty state shows structured section headers" apps/workbench/src/features/observatory/__tests__/observatory-drawer-panels.test.tsx
  </acceptance_criteria>
  <done>
    All 3 panel components render structured empty states with section headers, muted placeholders, and contextual hints. Test file has new assertions verifying the section headers and hint text are present. All existing tests still pass.
  </done>
</task>

</tasks>

<verification>
1. Run full panel test suite: `cd apps/workbench && npx vitest run src/features/observatory/__tests__/observatory-drawer-panels.test.tsx`
2. All existing tests (a-d per panel) pass unchanged
3. All new tests (e per panel) pass
4. Grep confirms each requirement's key content exists in the source files
</verification>

<success_criteria>
- EMP-01: ExplainabilityDrawerPanel empty state shows STATION, PRESSURE LANES, ANOMALIES section headers + "Click a station or press E while hovering" hint
- EMP-02: MissionDrawerPanel empty state shows BRIEFING, OBJECTIVES, NARRATIVE section headers + "Start a mission from the command palette" hint
- EMP-03: GhostMemoryDrawerPanel empty state shows explanatory text about "prior hunt findings" and "probing stations"
- All 15+ panel tests pass (12 existing + 3 new)
</success_criteria>

<output>
After completion, create `.planning/phases/34-panel-empty-states/34-01-SUMMARY.md`
</output>
