# hushd config for the EDR pipeline example
listen: "0.0.0.0:8080"
policy_path: "/etc/hushd/policy.yaml"
audit_db: "/var/lib/hushd/audit.db"
log_level: "info"

auth:
  enabled: true
  api_keys:
    - name: "default"
      key: "${CLAWDSTRIKE_API_KEY}"
      scopes: ["check", "read"]

