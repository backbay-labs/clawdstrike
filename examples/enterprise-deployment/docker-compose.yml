# Enterprise-ish hushd compose: signed policy bundles + auth + persisted audit

services:
  hushd:
    build:
      context: ../..
      dockerfile: Dockerfile.hushd
    ports:
      - "8080:8080"
    volumes:
      - ./config.yaml:/etc/hushd/config.yaml:ro
      - hushd-data:/var/lib/hushd
    environment:
      - CLAWDSTRIKE_API_KEY=${CLAWDSTRIKE_API_KEY:-changeme}
      - CLAWDSTRIKE_ADMIN_KEY=${CLAWDSTRIKE_ADMIN_KEY:-admin}
      - CLAWDSTRIKE_POLICY_BUNDLE_PUBKEY=${CLAWDSTRIKE_POLICY_BUNDLE_PUBKEY:-}
    healthcheck:
      test: ["CMD-SHELL", "hushd status http://127.0.0.1:8080 >/dev/null 2>&1 || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 20
    restart: unless-stopped

volumes:
  hushd-data:
