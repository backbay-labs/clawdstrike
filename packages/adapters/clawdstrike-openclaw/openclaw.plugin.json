{
  "id": "clawdstrike-security",
  "version": "0.2.7",
  "name": "Clawdstrike Security Plugin",
  "description": "Tool-layer security guardrails for OpenClaw agents (policy_check preflight + post-action tool output blocking/redaction). Provides policy-based access control decisions, secret detection, and egress allowlist guidance.",
  "main": "./dist/plugin.js",
  "configSchema": {
    "type": "object",
    "properties": {
      "policy": {
        "type": "string",
        "description": "Path to policy YAML file or built-in ruleset name (e.g., 'clawdstrike:ai-agent-minimal')"
      },
      "mode": {
        "type": "string",
        "enum": ["deterministic", "advisory", "audit"],
        "default": "deterministic",
        "description": "Enforcement mode: deterministic (block), advisory (warn), audit (log only)"
      },
      "logLevel": {
        "type": "string",
        "enum": ["debug", "info", "warn", "error"],
        "default": "info",
        "description": "Logging verbosity level"
      },
      "guards": {
        "type": "object",
        "description": "Enable/disable specific guards",
        "properties": {
          "forbidden_path": {
            "type": "boolean",
            "default": true
          },
          "egress": {
            "type": "boolean",
            "default": true
          },
          "secret_leak": {
            "type": "boolean",
            "default": true
          },
          "patch_integrity": {
            "type": "boolean",
            "default": true
          },
          "computer_use": {
            "type": "boolean",
            "default": true
          },
          "remote_desktop_side_channel": {
            "type": "boolean",
            "default": true
          },
          "input_injection_capability": {
            "type": "boolean",
            "default": true
          }
        }
      }
    },
    "required": []
  },
  "hooks": [
    {
      "event": "before_tool_call",
      "handler": "./dist/hooks/cua-bridge/handler.js"
    },
    {
      "event": "before_tool_call",
      "handler": "./dist/hooks/tool-preflight/handler.js"
    },
    {
      "event": "tool_call",
      "handler": "./dist/hooks/cua-bridge/handler.js"
    },
    {
      "event": "tool_call",
      "handler": "./dist/hooks/tool-preflight/handler.js"
    },
    {
      "event": "tool_result_persist",
      "handler": "./dist/hooks/tool-guard/handler.js"
    },
    {
      "event": "agent:bootstrap",
      "handler": "./dist/hooks/agent-bootstrap/handler.js"
    }
  ],
  "tools": [
    {
      "name": "policy_check",
      "handler": "./dist/tools/policy-check.js"
    }
  ],
  "commands": [
    {
      "name": "clawdstrike",
      "description": "Clawdstrike security management"
    }
  ]
}
