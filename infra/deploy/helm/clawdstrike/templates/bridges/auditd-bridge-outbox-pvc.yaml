{{- if and .Values.bridges.auditd.enabled .Values.bridges.auditd.outboxPersistence.enabled (eq .Values.bridges.auditd.outboxPersistence.type "pvc") .Values.bridges.auditd.outboxPersistence.pvc.create (not .Values.bridges.auditd.outboxPersistence.pvc.existingClaim) }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "clawdstrike.fullname" . }}-auditd-bridge-outbox
  namespace: {{ include "clawdstrike.namespace" . }}
  labels:
    {{- include "clawdstrike.componentLabels" (dict "ctx" . "component" "auditd-bridge") | nindent 4 }}
spec:
  accessModes:
    {{- range .Values.bridges.auditd.outboxPersistence.pvc.accessModes }}
    - {{ . | quote }}
    {{- end }}
  {{- if .Values.bridges.auditd.outboxPersistence.pvc.storageClassName }}
  storageClassName: {{ .Values.bridges.auditd.outboxPersistence.pvc.storageClassName | quote }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.bridges.auditd.outboxPersistence.pvc.size }}
{{- end }}
