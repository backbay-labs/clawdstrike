{{- if .Values.bridges.hubble.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "clawdstrike.fullname" . }}-hubble-bridge
  namespace: {{ include "clawdstrike.namespace" . }}
  labels:
    {{- include "clawdstrike.componentLabels" (dict "ctx" . "component" "hubble-bridge") | nindent 4 }}
spec:
  replicas: {{ .Values.bridges.hubble.replicas }}
  selector:
    matchLabels:
      {{- include "clawdstrike.componentSelectorLabels" (dict "ctx" . "component" "hubble-bridge") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "clawdstrike.componentLabels" (dict "ctx" . "component" "hubble-bridge") | nindent 8 }}
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      {{- include "clawdstrike.imagePullSecrets" . | nindent 6 }}
      serviceAccountName: {{ include "clawdstrike.serviceAccountName" . }}
      hostNetwork: {{ .Values.bridges.hubble.hostNetwork }}
      dnsPolicy: {{ .Values.bridges.hubble.dnsPolicy }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: hubble-bridge
          image: "{{ .Values.bridges.hubble.image.repository }}:{{ .Values.bridges.hubble.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          {{- $hubbleOutboxPath := .Values.bridges.hubble.env.OUTBOX_PATH }}
          {{- if .Values.bridges.hubble.outboxPersistence.enabled }}
          {{- $hubbleOutboxPath = printf "%s/%s" .Values.bridges.hubble.outboxPersistence.mountPath .Values.bridges.hubble.outboxPersistence.fileName }}
          {{- end }}
          ports:
            - name: admin
              containerPort: {{ .Values.bridges.hubble.adminPort }}
              protocol: TCP
          env:
            - name: NATS_URL
              value: {{ include "clawdstrike.natsUrl" . | quote }}
            - name: HUBBLE_ENDPOINT
              value: {{ .Values.bridges.hubble.grpcEndpoint | quote }}
            - name: ADMIN_LISTEN_ADDR
              value: "0.0.0.0:{{ .Values.bridges.hubble.adminPort }}"
            - name: RUST_LOG
              value: {{ .Values.bridges.hubble.env.RUST_LOG | quote }}
            - name: STREAM_MAX_BYTES
              value: {{ .Values.bridges.hubble.env.STREAM_MAX_BYTES | int64 | quote }}
            - name: STREAM_MAX_AGE_SECONDS
              value: {{ .Values.bridges.hubble.env.STREAM_MAX_AGE_SECONDS | int64 | quote }}
            - name: NATS_STARTUP_TIMEOUT_SECS
              value: {{ .Values.bridges.hubble.env.NATS_STARTUP_TIMEOUT_SECS | int64 | quote }}
            - name: OUTBOX_ENABLED
              value: {{ .Values.bridges.hubble.env.OUTBOX_ENABLED | quote }}
            - name: OUTBOX_PATH
              value: {{ $hubbleOutboxPath | quote }}
            - name: OUTBOX_FLUSH_INTERVAL_MS
              value: {{ .Values.bridges.hubble.env.OUTBOX_FLUSH_INTERVAL_MS | int64 | quote }}
            - name: OUTBOX_MAX_PENDING
              value: {{ .Values.bridges.hubble.env.OUTBOX_MAX_PENDING | int64 | quote }}
            - name: OUTBOX_RETRY_BASE_MS
              value: {{ .Values.bridges.hubble.env.OUTBOX_RETRY_BASE_MS | int64 | quote }}
            - name: OUTBOX_RETRY_MAX_MS
              value: {{ .Values.bridges.hubble.env.OUTBOX_RETRY_MAX_MS | int64 | quote }}
            - name: READINESS_OUTBOX_DEGRADED_THRESHOLD
              value: {{ .Values.bridges.hubble.env.READINESS_OUTBOX_DEGRADED_THRESHOLD | int64 | quote }}
            {{- if .Values.spiffe.enabled }}
            - name: SVID_PATH
              value: {{ .Values.spiffe.svidPath | quote }}
            {{- end }}
          {{- if or .Values.bridges.hubble.outboxPersistence.enabled .Values.spiffe.enabled }}
          volumeMounts:
            {{- if .Values.bridges.hubble.outboxPersistence.enabled }}
            - name: outbox
              mountPath: {{ .Values.bridges.hubble.outboxPersistence.mountPath | quote }}
            {{- end }}
            {{- if .Values.spiffe.enabled }}
            - name: spiffe-workload
              mountPath: /var/run/spire/agent
              readOnly: true
            {{- end }}
          {{- end }}
          livenessProbe:
            httpGet:
              path: /healthz
              port: admin
            initialDelaySeconds: 5
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /readyz
              port: admin
            initialDelaySeconds: 3
            periodSeconds: 10
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          resources:
            {{- toYaml .Values.bridges.hubble.resources | nindent 12 }}
      {{- if or .Values.bridges.hubble.outboxPersistence.enabled .Values.spiffe.enabled }}
      volumes:
        {{- if .Values.bridges.hubble.outboxPersistence.enabled }}
        - name: outbox
          {{- if eq .Values.bridges.hubble.outboxPersistence.type "hostPath" }}
          hostPath:
            path: {{ .Values.bridges.hubble.outboxPersistence.hostPath.path | quote }}
            type: {{ .Values.bridges.hubble.outboxPersistence.hostPath.type | quote }}
          {{- else if eq .Values.bridges.hubble.outboxPersistence.type "pvc" }}
          persistentVolumeClaim:
            claimName: {{ .Values.bridges.hubble.outboxPersistence.pvc.existingClaim | default (printf "%s-hubble-bridge-outbox" (include "clawdstrike.fullname" .)) }}
          {{- else }}
          emptyDir: {}
          {{- end }}
        {{- end }}
        {{- if .Values.spiffe.enabled }}
        - name: spiffe-workload
          csi:
            driver: "csi.spiffe.io"
            readOnly: true
        {{- end }}
      {{- end }}
      {{- with .Values.bridges.hubble.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.bridges.hubble.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
